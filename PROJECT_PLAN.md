# AI Waifu System — 1차 완성 계획과 검수 기록

저장소 반영 범위: 원격 main의 기존 README 갱신과 SBV2 소스 삭제 이력을 유지한다.
앱 변경·Gemini 어댑터·실행 안내·본 계획·Git 제외 규칙·자동 테스트 7개 파일만 반영한다.
기존 tests 삭제 이력 이후 검증된 66개 테스트를 재현하기 위해 해당 테스트 파일들을 다시 포함한다.
SBV2의 torch_dtype 호환성 수정은 개인 설치에 유지하고 README에 방법만 기록한다.
모델·데이터·인증 정보·개인 TTS 설정·임시 검수기·백업·빈 requirements.txt·줄바꿈 전용 변경은 제외한다.
이 문서 아래의 경로와 과거 HEAD는 당시 로컬 검수 환경 기록이며 원격 배포 구성 전체를 뜻하지 않는다.

실행기 경로 갱신: 재부팅 후 임시 실행기 소실이 반복되어 `local_checks/check_conversation.py`에 검수 실행기를 보관한다. `.gitignore`에 `local_checks/`를 추가했고 Git 제외 및 구문 검사를 확인했다. 기존 `/private/tmp/ai-waifu-context-VuUhYG/check_conversation.py` 실행 명령은 더 이상 사용하지 않는다. 키는 숨김 입력 후 프로세스 환경변수에서만 사용하며 종료 시 제거한다. WAV만 임시 폴더에 저장한다. 앱·SBV2 구현은 변경하지 않았고 실제 Gemini 사전 확인 및 수정 후 4턴 검수는 사용자 실행 대기다. 재부팅에 따른 삭제는 사용자 추정이며 삭제 이력으로 확정하지 않았다.

최종 갱신: **2026-09-08, Asia/Seoul**. 마감: **2026-12-31**. 작업 가능 시간: **미정**.
기준 작업 폴더: `/Users/eonpisa_1017/Desktop/AI_Waifu_System`, 브랜치: `main`, HEAD: `bd1a680`.
판단 기준은 이 작업 폴더의 현재 파일과 이번 조사에서 실행한 결과다. HEAD만의 상태나 README의 초록 표시를 완료 증거로 사용하지 않는다.

**최근 상태:** 일반 main.py 실행 기준 검수 완료. 키 숨김 입력 후 KO→JP·일본어 응답·Gemini 지정 모델/폴백 없음·정상 한국어 자막·SBV2 정상 음성·다음 입력 복귀를 확인했다. Q05-B 4턴 완료도 유지한다. 영구 기억은 구현하지 않았다. 실행 기준과 결과는 26절.

## 1. 목표와 범위

한국어 키보드/음성 입력 → 필요한 입력 보정·일본어 번역 → 성격과 말투를 유지한 일본어 대화 → 일본어 검증·재생성 → 한국어 자막 → 개인 SBV2 음성 → 상황에 맞는 Live2D 표정 → 저장·기본 기억 → 실패 후 다음 대화까지 이어지는 개인용 1차 완성본.

| 구분 | 이번 범위 | 이유 |
| --- | --- | --- |
| 필수 | 키보드, 누르는 동안 말하기 방식의 한국어 음성 입력, 보수적 보정, 일본어 응답·검증, 한국어 자막, 개인 SBV2 음성, 기본 표정, 로컬 저장, 재시작 후 기본 기억, 오류 복구 | 사용자가 지정한 전체 흐름을 실제로 시연할 수 있어야 한다. |
| 축소 가능한 구현 깊이 | 음성 입력은 수동 녹음 시작/종료, 자막은 현재 콘솔 표시, 표정은 중립·기쁨·슬픔 중심, 기억은 최근 대화와 명시적으로 기억한 사실 몇 개, 한 가지 주 실행 구성 | 핵심 기능을 유지하면서 상시 청취, 별도 자막 창, 복잡한 감정 추론, 여러 장비 동시 인증의 작업량을 줄인다. 사용자와 범위를 확정한다. |
| 후속 개발 | 상시 청취·자연스러운 끼어들기, 세밀한 모션·립싱크 개선, 벡터 DB/RAG, 자동 장기 요약·기억 추출, 배포 패키지·여러 사용자 | 기본 흐름의 안정화 이후 진행한다. 새 DB 서비스나 유료 도구는 도입하지 않는다. |
| 제외·보류 | VR, 실제 로봇, CosyVoice 재도입, 음성 모델 재학습·교체·공개 | VR/로봇은 장기 목표이고 CosyVoice는 보류 상태다. 기존 학습 모델을 활용한다. |

Gemini는 기존 선택형 외부 번역 경로로 유지한다. 키가 없거나 실패하면 로컬 Qwen으로 진행한다. 새 유료 API나 STT 모델을 정할 때에는 도입 이유, 비용, 저장 공간·메모리 영향을 먼저 설명한다. 이번에는 유료 API 호출, 모델 다운로드·교체, 원격 반영을 하지 않았다.

## 2. 최종 결과물과 검수 문턱

구현 완료와 실환경 검수 완료를 따로 기록한다. 자동 테스트만 통과한 작업은 사용자 청취·마이크·Live2D 검수를 완료 처리하지 않는다.

| 검수 ID | 결과물·시연 | 통과 기준 |
| --- | --- | --- |
| D01 | 실행 안내, 장비별 설정, 실행 경로·서버 진단 결과 | 선택한 주 장비에서 안내만으로 실행. 다른 작업 디렉터리에서도 같은 코드·설정·음성 파일을 사용. 잘못된 서버/모델/SSD 경로는 구체적으로 안내. |
| D02 | 키보드로 10턴 대화 | 인사·오타·고유명사·짧은 답·복수 문장 포함. 비정상 종료 0회. 일본어만 음성에 전달하고 한국어 자막 표시. |
| D03 | 음성 입력 평가 20문장 | 녹음·전사 결과를 확인하고 재입력 가능. 의미 보존 18/20 이상을 초기 제안 기준으로 삼아 실제 마이크 평가 때 확정. 무음·권한 거부·장치 분리 후 키보드 사용 가능. |
| D04 | 번역/성격 평가표와 단계별 시간 원본 | 20개 고정 입력의 의미·고유명사·부정/시제·말투 평가. 의미 보존 18/20 이상, 지정 이름/부정 의미 반전 0회를 초기 제안. 자동 혼합 언어 실패 사례는 100% 차단. 지연 목표는 Q02 측정 뒤 확정. |
| D05 | 개인 SBV2 모델로 일본어 3문장과 긴 문장 재생 | 지정 체크포인트·화자·스타일 일치, 유효 WAV, 사용자가 무음·잡음·문장 잘림이 없음을 청취 확인. 실패 시 예전 WAV를 재생하지 않음. |
| D06 | Live2D 기본 표정 시연 | 중립/기쁨/슬픔이 모델에서 구별되고 끝나면 중립으로 복귀. VTS 종료·인증 거부에도 텍스트 대화 지속. |
| D07 | 로컬 대화 저장과 재시작 후 기억 | 정상 대화 저장, 검증 실패 응답은 성공 대화로 저장하지 않음. 재시작 후 지정 사실 3개 활용, 수정·잊기 반영, 다른 세션 사실 임의 생성 없음. |
| D08 | 장애 주입 결과표 | Ollama 불통, 잘못된 번역, Gemini 실패, SBV2 403/timeout/잘못된 본문, 오디오 실패, VTS 불통, STT 실패, 저장 실패 각각 안내 후 다음 입력 가능. 재시도 횟수 유한. |
| D09 | 최종 실행 기록과 결함 목록 | D01–D08 통과, 재시작 시연 2회 및 30턴 연속 시연에서 미처리 예외 0회. 데이터 손실·잘못된 음성 재생 등 차단 결함 0건. 알려진 제한을 문서화. |

품질 통과 비율은 **목표 제안**이지 현재 달성 수치가 아니다. 캐릭터 말투와 음질은 자동 정규식이나 코드 조사만으로 판정하지 않는다.

## 3. 현재 구현과 실제 연결

현재 실행 흐름:

```text
main.py: input()
  → emotion.detect_emotion(한국어 입력, "")
  → translator.translate_korean_input()
  → japanese_response.generate_validated_japanese_reply()
  → translator.japanese_to_korean(): Gemini 1회 → 실패/검증 거부 시 Qwen 최대 2회
  → subtitle_ui.format_korean_subtitle(): 콘솔 print
  → emotion.clean_for_tts / apply_emotion_to_tts / limit_text(80자)
  → tts.speak(): SBV2 → 현재 설정상 CosyVoice fallback 가능
  → audio_playback.play_wav("output.wav")
  → 메모리 안의 messages 목록 갱신
```

| 기능 | 코드 존재 | 메인 연결 | 이번 정상 확인 | 남은 검수/문제 |
| --- | --- | --- | --- | --- |
| 키보드·종료·기록초기화 | 있음, `main.py` | 연결 | 가상환경으로 시작→종료 성공. 복구 흐름은 모의 실행 | `main()` 보호절 없음, import 시 입력 시작. EOF/Ctrl+C 정리 필요. |
| 입력 보정·KO→JA | 있음, `translator.py` | 연결 | 실제 `안녕→こんにちは` 1건, 모의 테스트 | 의미 품질 미측정. 가나 포함/한자 입력 우회 검증 결함 재현. |
| Ollama 일본어 대화·페르소나 | 있음, `llm.py`, `main.py` | 연결 | `qwen3.5:9b` 실제 응답 1건 | 말투 지속성 미검수. 시스템/사용자 스타일 지시가 한국어로 구성돼 있음. |
| 일본어 검증·재생성 | 있음, `japanese_response.py` | 연결 | 기존 모의 테스트, 실제 대화 중 1회 재생성 후 통과 | 영문 한 글자, 한국어 자모, 일부 마크다운 통과 재현. 중국어 완전 판별/의미 품질을 보장하지 않음. |
| Gemini→Qwen 자막 | 있음, `gemini_translator.py`, `translator.py` | 연결 | 모의 Gemini 성공/실패/잘못된 결과, 실제 키 없음→Qwen 성공 | 현재 프로세스에 키 없음. 실제 Gemini 모델/API 사용 가능 여부·품질 미검수. 기본 모델 문자열은 `gemini-3.5-flash-lite`. |
| 한국어 자막 | 있음, `subtitle_ui.py` | 콘솔 연결 | 형식 모의 테스트, 실제 번역 문자열 확인 | 별도 자막 GUI/Live2D 오버레이는 없음. |
| SBV2 요청 | 있음, `tts.py` | 연결 | HTTP 요청 형태 모의 테스트만 정상 | 현재 5000번은 AirTunes. 실제 SBV2 합성 미확인. 비오디오 본문 저장 결함. |
| WAV 재생 | 있음, `audio_playback.py` | 연결 | 누락 파일·Windows 호출 모의 테스트 | Mac `afplay` 명령 존재. 실제 재생·청취 미실행. 저장은 프로젝트 기준, 재생은 현재 디렉터리 기준. |
| 감정 | 한국어 키워드 규칙·7개 프리셋, `emotion.py` | TTS 속도/assist_text에 연결 | 코드 조사 | 응답 감정/맥락 반영 없음. 사용자 ‘슬퍼’가 곧 캐릭터 슬픔으로 연결되는 단순 규칙. |
| VTS/Live2D | 토큰 인증·파라미터 주입, `vts.py` | **import/래퍼 정의만 있음. 호출 없음** | TCP 8001 연결 거부 | 토큰 존재만 확인. 토큰 내용 미열람. 실제 파라미터·표정·재연결 미검수. |
| 음성 입력 | `stt.py` 빈 파일 | 없음 | 없음 | 녹음·STT 모델·입력 선택 전부 구현 예정. |
| 현재 세션 문맥 | `messages`, `MAX_HISTORY=12` | 연결 | 실패 턴 제외/다음 턴 진행을 모의 실행 | 시스템 외 최대 12개 메시지, 대략 6턴. 사용자 메시지에 한국어 지시문 포함. 영구 저장 아님. |
| 대화 저장·기본 기억 | 없음 | 없음 | 없음 | 파일/DB 저장, 검색·주입, 재시작 후 복구 구현 예정. |
| CosyVoice | 지연 import되는 선택형 코드 | 설정에 따라 연결 | 미검수 | 현재 fallback=true, 설정 없을 때 기본 backend도 cosyvoice. 보류 요구와 불일치. 코드의 기본 모델은 300M이므로 CosyVoice 2 실연동 완료로 간주하지 않음. |
| 오류 복구 | 번역/응답/TTS/재생의 일부 처리 | 부분 연결 | 대표적인 실패 반환값 후 다음 턴 모의 통과 | 최상위 턴 예외 경계 없음. 잘못된 TTS 설정으로 종료 가능. 전체 요청 기한/취소 없음. |

## 4. 환경·작업 트리 조사

- 적용되는 `AGENTS.md` 및 기존 작업 관리 문서는 검색 범위에서 발견하지 못했다. README 외 상세 계획이 없어 이 문서를 생성했다.
- 기존 수정 11개: `SBV2-KR-master/config.py`, `slice.py`, `transcribe.py`, 루트 `ai.py`, `emotion.py`, `llm.py`, `main.py`, `translator.py`, `vts.py`, `tests/test_llm.py`, `tests/test_translator.py`.
- `git diff --ignore-space-at-eol --numstat`에서는 `llm.py`, `translator.py`, 두 테스트 파일의 내용 차이가 남는다. 나머지 변경 표시를 불필요하게 되돌리거나 줄바꿈을 정리하지 않았다.
- `git diff --check`는 기존 수정 파일들의 CRLF/줄 끝 공백 때문에 실패했다. 이번 문서 작성으로 전체 작업 트리의 형식 검사가 통과했다고 보고하지 않는다. 기존 변경의 일괄 줄바꿈 수정은 하지 않았다.
- README, requirements, Gemini 어댑터, 설정, 토큰, 두 SBV2 트리/학습 파일 등에 미추적 파일이 많았다. 커밋·삭제·이동·모델 교체·원격 반영 없음. `Style-Bert-VITS2`는 내부 `.git`도 있는 별도 트리다.
- 루트 `requirements.txt`는 빈 파일이다. SBV2 쪽 requirements/pyproject는 별도로 있으며 앱 클라이언트 의존성과 구분해야 한다.
- 현재 호스트: macOS 26.5 arm64, Python 3.11.0. 하드웨어 모델명·메모리 사용량·GPU 사용률은 이번에 측정하지 않았다. Ryzen/RTX/M5 사양은 사용자 제공 배경이다.
- `.venv/bin/python`은 실행되며 requests 2.34.2, websockets 12.0, torch 2.5.1 메타데이터 확인. 세 모듈과 `pyopenjtalk` 실제 import 성공. 단, pyopenjtalk 배포 메타데이터 조회는 실패했고 import에는 pkg_resources 경고가 있다. 패키지 누락으로 단정하지 않는다.
- 루트 `.venv/pyvenv.cfg`에는 Windows 생성 경로가 남아 있는데 Mac 실행 파일/패키지도 존재한다. 현재 실행은 가능하지만 깨끗한 재현 환경으로 검증되지 않았다. 기존 환경을 덮어쓰지 않고 클라이언트 환경을 분리할 계획이다.
- 시스템 `python3`은 requests만 확인되고 websockets가 없어 메인이 import 단계에서 실패한다. `python`, `pytest`, `uv` 명령은 현재 PATH에 없음. 테스트는 표준 `unittest`로 실행 가능하다.
- `Style-Bert-VITS2/.venv`에는 Windows 설정만 확인되고 `.venv/bin/python`은 없다. 해당 트리의 `model_assets`에는 모델 디렉터리가 없다.
- `SBV2-KR-master/model_assets`에는 9개 모델 디렉터리가 있다. 현재 TTS 설정이 지정한 체크포인트(251,187,844 bytes), config.json, style_vectors.npy가 존재하고 화자/Neutral 이름이 config에 있다. **파일 존재를 모델 로드·합성 성공으로 간주하지 않는다.**
- 현재 `/Volumes`에는 Macintosh HD만 표시된다. 이 위치의 개인 모델 파일은 발견됐지만 외장 SSD 연결·데스크톱 모델 준비 상태는 미확인이다.
- 해당 프로세스에 OLLAMA/SBV2/TTS/GEMINI/AI_WAIFU 설정 환경변수는 없다. API 키 값과 VTS 토큰 내용은 출력하지 않았다.
- Ollama 기본 URL은 `127.0.0.1:11434/api/chat`, 대화/번역 모델 기본값은 `qwen3.5:9b`. 실제 `/api/tags` 200과 해당 모델 존재 확인. 다른 모델을 다운로드하거나 바꾸지 않았다.
- TTS 로컬 설정은 `backend=sbv2`, `fallback_to_cosyvoice=true`, `127.0.0.1:5000`, JP/Neutral. SBV2-KR 서버 `config.yml`도 port=5000. 서버 코드는 CUDA 불가 시 CPU를 선택하며 이 실행 경로에 MPS 선택은 없다.
- 코드/로그 검색에서 현재 프로젝트의 `.log` 파일이나 백업 폴더 실행 설정은 발견하지 못했다. 이번 클라이언트 모듈의 `__file__`은 모두 작업 폴더 루트였다. 별도 사용자의 IDE/이전 서버 실행 경로까지 검증한 것은 아니다.
- 사용하지 않는 `ai.py`는 메인에서 참조되지 않고 현재 환경에 없는 openai 모듈을 사용한다. `tempCodeRunnerFile.py`는 실제 SyntaxError가 있지만 메인 흐름에서 참조되지 않는다. 삭제하지 않았다.

## 5. 이번 실행 증거 (2026-09-07)

| ID | 실행/조건 | 결과 | 해석·한계 |
| --- | --- | --- | --- |
| E01 | `.venv/bin/python -m unittest discover -s tests -v` | **53개, 0.031초, 모두 통과** | 현재 코드로 새로 실행. 외부 모델/음성/VTS 품질을 검증하지 않는 모의 테스트 중심. SBV2 벤더 테스트는 실행하지 않음. |
| E02 | 가상환경으로 `main.py`, stdin에 시작 Enter와 `종료` 전달 | exit 0 | 시작·명시적 종료 확인. 정상 대화 전체 실검수 아님. |
| E03 | 시스템 `python3 main.py`, 동일 입력 | exit 1, `ModuleNotFoundError: websockets` | 실행 인터프리터에 따라 실제 시작 결과가 달라짐. |
| E04 | `runpy.run_path('main.py')`, 입력/외부 함수 mock | 입력 번역 실패 후 다음 턴, 응답 검증 실패 후 다음 턴, 자막·TTS 실패 후 다음 턴 통과 | 각각 최종 history 크기 3/3/5. TTS 실패 턴은 오디오 호출 0회. 재사용 가능한 통합 테스트로 옮기는 작업은 P05/P06. |
| E05 | 같은 main 모의 실행에 `ValueError`를 TTS에서 발생, stdin EOF | 각각 미처리 ValueError / EOFError | 함수가 False/None을 돌려주는 경우와 예상 밖 예외를 구분해야 함. |
| E06 | 로컬 `/api/tags`, 포트 조회 | Ollama 200. VTS TCP 8001 연결 거부 | 데스크톱/원격 서버에는 접근하지 않음. 최초 sandbox 차단 후 허용된 호스트 조회로 확인. |
| E07 | `lsof` 5000, GET `/models/info`, POST `/voice` | ControlCenter PID 632. GET 200, `AirTunes/950.7.1`, Apple binary plist. POST **403**, 동일 Server 헤더 | **현재 403 재현, 응답 주체는 SBV2가 아님.** SBV2 내부 권한/모델 오류의 증거가 아니다. OS 서비스를 종료하거나 포트를 변경하지 않음. |
| E08 | 실제 Ollama: `안녕`, 현재 main 페르소나/스타일과 모듈 함수 사용 | KO→JA **11.160초**, 일본어 응답(1회 재생성 포함) **6.175초**, JA→KO Qwen **3.890초**, 합계 **21.225초** | n=1, warm/cold 상태 통제 없음. Gemini 키 없음. TTS·재생·VTS·STT 제외. 평균/p95/최대치나 목표 속도가 아님. |
| E09 | 입력 우회 검사: `こんにちは 안녕`, `你好` | 번역 호출 없이 status=ok, 원문 그대로 반환 | `is_japanese`를 언어 판정·검증 완료로 사용한 결과. Q03 대상. |
| E10 | 응답 검증: `こんにちは A`, `こんにちは ㅋㅋ`, 일본어 뒤 `# 見出し` | 모두 검증 통과(None) | 단일 Latin 문자·한글 자모·마크다운 누락. Q04 대상. |
| E11 | 임시 폴더의 `_write_wav_atomically`에 JSON 바이트 전달 | True, 비WAV도 저장 | 사용자 기존 output.wav는 건드리지 않음. P07 대상. |
| E12 | `_read_config`를 `{'backend':'sbv2','sbv2':[]}`로 mock 후 speak | AttributeError | 설정 형태 검증과 턴 예외 경계 필요. P02/P06 대상. |
| E13 | pyopenjtalk 실제 import | 성공, 비권장 API 경고 | 작업 서버 초기화/7861 RPC 및 실제 g2p·합성은 실행하지 않음. 과거 worker 연결 실패의 재발 여부 **미확인**. |
| E14 | 루트·tests Python AST 파싱 | tempCodeRunnerFile.py만 SyntaxError | 벤더 전체의 컴파일/학습 테스트 결과가 아님. |

E08 일본어는 `こんにちは。今日も元気ですか？`로 시작하는 3문장이고 한국어 자막도 생성됐다. 인사 1건은 문맥 유지나 전체 번역 품질의 대표 표본이 아니다. 예전 `output.wav`와 학습 음성은 청취·재생하지 않았다.

## 6. 현재 장애물과 가장 먼저 할 작업

1. **P04 완료 → Q06 Gemini 연결·4턴 검수 우선**: 사용자 요청에 따라 정상 SBV2 흐름을 유지하면서 자막 경로를 먼저 확인한다. 단건 번역 함수는 P05 메인 함수 분리 없이 실행할 수 있다. P05/P06 안정화는 그 뒤에 진행하며, 이번 작업을 함수 분리·장애 복구 전체 완료로 취급하지 않는다.
2. **남은 검수**: `/g2p`의 누락 import는 별도 결함으로 남는다. 현재 메인은 사전 지정한 입력 1턴만 검증했으므로 연속 10턴, 입력 장치 조작감, 장애 후 복구, 다양한 말투/번역 품질은 완료로 처리하지 않는다.
3. **P01 실행 기준 고정**: 빈 requirements와 Windows/Mac 패키지가 공존하는 가상환경의 재현성 문제는 남는다. 현재 확인한 Mac Python을 유지하고 이번에는 패키지를 재설치하지 않았다.

이번 SBV2 검수 구성은 **Mac에서 앱 TTS 모듈과 SBV2를 함께 실행**한 것으로 확정됐다. 최종 주 실행 구성과 앞으로 2주의 작업 가능 시간은 미정이다. 데스크톱 준비나 추가 작업 시간을 확보했다고 간주하지 않는다.

## 7. 작업 카드

표의 시간은 사용자와 Codex가 함께 진행하는 집중 개발 세션의 초기 추정이며, 사용자 검수도 포함한다. 무인 다운로드/학습 대기는 제외하고 별도 장애물로 기록한다. 사용자 가능 시간을 이 수치로 임의 배정하지 않는다. 각 카드는 약 0.5–2시간 안에 결과를 확인하는 단위다. 예상 상한을 넘기면 같은 작업을 무작정 연장하지 않고 원인과 후속 카드를 기록한다.

우선순위: P0=안정화/검수 차단, P1=1차 완성 필수 확장. 불확실성: 낮음=외부 환경 없음, 중간=기존 코드 수정/품질 조정, 높음=장비·모델·실물 검수 의존.
상태 표기: `구현 / 실환경 검수`. 아래 36개 작업의 최초 상태는 **대기 / 미검수**이며, 현재 P02/P03/P04의 부분 완료와 남은 검수는 12절 상태표를 따른다. 기존 기능 일부가 있어도 카드의 통과 기준을 만족해야 완료로 바꾼다.

### P — 현재 흐름과 실행 환경 안정화

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **P01 실행 기준 고정** — 인터프리터 혼선을 막는 클라이언트 requirements, 실행 안내, 비밀값 없는 진단 명령 | P0 / A00 / 1–2h / 중간 | C: 실제 import 기준 의존성 분리, Python·cwd·모듈·설정 경로 출력 구현. U: 첫 장비/서버 배치 선택 | 기존 환경 보존. 깨끗한 별도 클라이언트 환경에서 설치·import·시작→종료 성공. 현재 53개 테스트 통과. 루트/다른 cwd에서 같은 모듈을 가리킴. 재현 환경 설치가 막히면 구현만 완료, 검수 대기로 표시. |
| **P02 SBV2 기본값·설정 검증** — 보류된 CosyVoice가 자동 의존성이 되지 않도록 기본 SBV2/명시적 선택 정책과 설정 오류 안내 | P0 / P01 / 0.5–1.5h / 낮음 | C: 설정 누락·JSON 오류·잘못된 자료형/숫자 처리, 기존 로컬 fallback 비활성화. U: 별도 작업 없음 | 설정 없음/깨짐/`sbv2=[]`/잘못된 timeout을 시험. 미처리 예외와 CosyVoice 자동 import 0회. 명시적으로 선택하는 기존 CosyVoice 경로는 보존. |
| **P03 SBV2 서버·worker 식별** — 403 오진과 오래된 실행 경로 방지. 확정한 포트/모델 경로 및 진단 결과 | P0 / P01,P02 / 1–2h / 높음 | C: 잘못된 서비스 응답 식별, 해당 코드의 시작 절차와 서버 로그 조사. U: 필요 시 데스크톱/SSD·서버 실행 | `/models/info`가 SBV2 JSON이고 모델/화자/스타일 일치. Python·cwd·style_bert_vits2 경로 기록. worker 초기화와 짧은 `/g2p` 성공. AirTunes 200/403을 정상 서버로 인정하지 않음. 환경 복구가 2h를 넘기면 원인별 후속 카드로 재산정. |
| **P04 개인 음성 3문장 청취** — 모델 파일 존재를 실제 음성 성공과 구분. 별도 검수 WAV·결과표 | P0 / P03 / 0.5–1.5h / 높음 | C: 지정 모델 요청과 별도 출력 파일, 파형/길이 확인. U: 인사·질문·감정 문장을 직접 청취 | 체크포인트·화자 일치, 3/3 유효 오디오. 사용자 무음/잡음/명백한 발음·문장 누락 판정 기록. 기존 output.wav 보존. 실패는 모델·worker·HTTP·재생 단계로 구분. |
| **P05 대화 한 턴 함수 분리** — 메인을 테스트할 수 있도록 최소한의 `main()`/턴 함수와 통합 회귀 테스트 | P0 / P01 / 1–2h / 중간 | C: 기존 파이프라인/페르소나를 유지해 함수 분리. U: 기존 키보드 사용감 확인 | import 시 입력/네트워크 호출 없음. 정상 2턴, 입력 실패, 응답 거부, 기록초기화, 종료 순서 검수. 실패한 입력/응답은 성공 history에 남지 않음. 기존 테스트 통과. |
| **P06 실패 후 다음 턴 보장** — 예상 밖 예외도 세션을 종료하지 않도록 단계별 오류 결과와 종료 처리 | P0 / P02,P05 / 1–2h / 중간 | C: 예외 경계·단계 표시, EOF/Ctrl+C 정리 구현. U: 강제 중단 시 조작 확인 | 번역/LLM/TTS/재생에 예외를 각각 주입하고 뒤에 정상 입력 1건 성공. 실패 원인 단계가 표시되고 오래된 답/음성 재사용 없음. Ctrl+C/EOF에 traceback 없이 종료. |
| **P07 WAV 검증·경로 일치** — 비오디오 저장과 다른 폴더의 예전 파일 재생 방지. 검증된 경로 반환·문장 분할 | P0 / P02,P05 / 1–2h / 중간 | C: 유효 WAV 확인, 생성·재생 경로 통일, 80자 무조건 잘림을 문장 단위 처리로 교체. U: 긴 문장 끝 청취 | 200+HTML/JSON, 빈 바이트, 깨진 WAV 거부. 실패 후 이전 WAV 호출 0회. 다른 cwd에서 생성된 동일 파일 재생. 80자 초과 응답의 끝이 누락되지 않으며 서버 길이 제한 준수. |
| **P08 단계별 시간 기록** — 추측 대신 측정하기 위한 로컬 JSONL/CSV 계측 | P0 / P05,P06 / 1–2h / 낮음 | C: monotonic clock, 턴 ID·단계·재시도·성공 여부·시간 기록, 기본 원문/키 제외. U: 별도 작업 없음 | 입력 번역/대화/자막/TTS/재생 및 전체 시간을 구분. 실패·재생성도 기록. 인위적 지연 1건으로 측정 검증. 음성 대기와 생성 지연 혼동 없음. |

### Q — 번역·일본어·성격·지연 기준 수립

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **Q01 고정 평가 입력 20개** — 개선 전후를 비교할 입력·평가표 | P0 / A00 / 1–2h / 중간 | C: 인사/오타/고유명사/자모/모호함/부정/시제/긴 문장 사례와 기대 의미 작성. U: 말투 기준과 기대 의미 확인 | 20개에 고유 ID, 기대 의미, 이름/시제/부정 조건, 허용 변형, 판정란 존재. 정답 문자열 일치만으로 의미를 평가하지 않음. 사적인 학습 데이터를 재사용하지 않음. |
| **Q02 주 장비 기준 측정** — 장비별 가능한 목표를 정할 실제 품질·시간 표 | P0 / P04,P08,Q01 / 1–2h / 높음 | C: 첫 요청/예열 후 20건, 재시도·메모리/GPU 관측 가능한 값 기록, p50/p95 계산. U: 번역·응답 의미/말투 평가 | 모든 표본과 실패를 보존. cold 1건과 warm 20건을 분리하고 TTS 포함/제외 시간 분리. 관측 못 한 자원값은 미측정. 두 번째 장비 결과를 추정해서 채우지 않음. |
| **Q03 입력 우회·보정 경계 수정** — 혼합 입력과 의미 변형을 차단하는 입력 검증 | P0 / P05,Q01 / 1–2h / 중간 | C: 가나 포함만으로 우회하지 않게 수정, 한국어 자모·단일 Latin·모호함 사례 회귀 테스트. U: 고친 입력의 의미 확인 | E09 혼합/중국어 입력이 검증 없이 ok가 되지 않음. 정상 한국어 오타와 이름은 보존. 애매한 입력은 재질문. 실패 후 다음 한국어 입력 성공. |
| **Q04 일본어 응답 경계 수정** — 검증 밖 문자가 TTS에 도달하지 않도록 거부/재생성 규칙 보강 | P0 / P05,Q01 / 1–2h / 중간 | C: E10의 자모·단일 Latin·마크다운을 처리하고 정상 일본어 회귀 검사. U: 지나치게 거부되는 정상 표현 검토 | 지정 실패 표본 전부 거부, 정상 표본 통과. 최초+재생성 최대 2회. 2회 실패하면 자막/TTS/성공 history 호출 0회. 단순 문자 규칙을 완전한 언어 판별로 설명하지 않음. |
| **Q05 페르소나·문맥 10턴** — 단발 응답에서 성격 지속성으로 확장. 프롬프트와 평가 기록 | P1 / Q02,Q03,Q04 / 1–2h / 중간 | C: 일본어 사용자 본문과 스타일 지시 분리, 말투/길이 조정, history 쌍 유지. U: 차분함·짧은 표현·절제된 감정 3기준 평가 | 10턴에서 지정 기준 충족 9턴 이상을 제안 기준으로 기록. 번역 자막을 history에 넣지 않음. 6턴 이상에서도 역할 순서 정상. 불합격 사례는 구체 결함으로 남김. |
| **Q06 Gemini 실호출·대체 경로** — 실제 API 성공과 실패 대체를 분리 검수 | P0 / P05,Q01 / 0.5–1.5h / 높음 | C: 키 미설정/timeout/잘못된 JSON/혼합 결과 회귀, 키 준비 시 짧은 실제 번역. U: 기존 키를 본인 환경에 설정(채팅 공유 불필요) | 실키 사용 시 검증된 한국어 결과와 Gemini 성공 로그. 실패 시 Qwen만 유한 재시도. 둘 다 실패해도 일본어를 한국어 자막이라 표시하지 않고 TTS/다음 턴 지속. 키 없으면 실Gemini 검수만 대기. |
| **Q07 응답 예산·취소 조정** — 기본 120초 직렬 호출이 누적되는 대기를 제어 | P0 / P08,Q02,Q06 / 1–2h / 중간 | C: 장비 프로필별 연결/응답/전체 턴 기한과 취소, 필요 시 자막/TTS 처리 순서 조정. U: 기다릴 수 있는 수준 확인 | Q02 표를 근거로 목표 p50/p95와 최대 대기 T를 문서에 확정. 무응답 서비스 주입 시 T+테스트 허용 오차 1초 안에 제어 복귀. 최적화 후 동일 표본 재평가, 의미/음성 누락이 늘지 않음. |

### S — 한국어 음성 입력

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **S01 STT 파일 전사 어댑터** — 한국어 전사 모델 후보를 작은 시험으로 확정 | P1 / Q07 / 1–2h / 높음 | C: 무료/로컬 후보의 장비·의존성·다운로드 크기를 확인해 설명, 선택 모델의 파일→한국어 인터페이스. U: 모델 준비가 필요한 경우 다운로드/샘플 발화 | 3개 공개용/검수용 짧은 문장의 전사 성공과 시간/메모리 기록. 설치 실패/모델 없음은 명확한 결과로 반환. 새 모델 도입 결정 전에는 의존성을 필수화하지 않음. |
| **S02 수동 마이크 녹음** — 상시 청취 없이 입력을 통제하는 녹음 시작/종료 | P1 / S01 / 1–2h / 높음 | C: 장치 선택·녹음 한도·무음/권한 실패 처리. U: 마이크 권한·장치 조작 | 3회 녹음→종료→전사 재사용. 무음/권한 거부/장치 분리 시 무한 대기와 종료 없음. 검수 음성은 로컬 임시 파일, 기본 자동 영구 보관 없음. |
| **S03 공통 입력 흐름 연결** — 음성과 키보드가 같은 번역·검증을 사용하도록 통합 | P1 / S02,P05,Q03 / 1–2h / 중간 | C: 입력 모드·전사문 확인/취소·재입력 연결. U: 전사 오인식 수정 조작 | 같은 한국어 문장으로 두 입력 방식의 후속 호출 동일. 전사 취소/실패는 history·LLM에 들어가지 않음. 음성 실패 뒤 키보드 1턴 성공. |
| **S04 자기 음성 재입력 방지** — TTS를 사용자 말로 오인하지 않도록 녹음/재생 상태 관리 | P1 / S03,P07 / 1–2h / 중간 | C: 재생 중 녹음 제한과 종료 시 장치 정리. U: 스피커/헤드폰으로 확인 | 재생 중 의도치 않은 대화 턴 0회, 재생 후 다시 녹음 가능. 녹음 중 종료·취소 시 장치 해제. 자연스러운 끼어들기 구현은 후속. |
| **S05 마이크 20문장 검수** — STT 품질과 복구를 사용자 환경에서 승인 | P1 / S04,Q01 / 1–2h / 높음 | C: 전사/보정 전후·시간 비교표 작성. U: 20문장 발화와 의미 판정 | D03 기준 평가, 고유명사/짧은 답/생활 소음 포함. 실패 문장은 원인 분류. 미달이면 모델 교체를 자동 수행하지 않고 데이터와 선택지 제시. |

### V — 감정과 캐릭터 표현

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **V01 모델 파라미터 매핑** — 하드코딩한 표정 ID가 실제 Live2D에 맞는지 확인 | P1 / P01 / 1–2h / 높음 | C: VTS 모델/파라미터 조회와 중립·기쁨·슬픔 매핑 작성. U: VTS 실행·API 허용·Live2D 모델 선택 | 3개 프리셋이 존재하는 ID/허용 범위만 참조. 없는 파라미터를 보고하고 건너뜀. 인증 토큰은 로컬 제외 경로 사용. |
| **V02 VTS 연결·인증 복구** — 무한 recv 대기와 연결 단절 방지 | P1 / V01 / 1–2h / 높음 | C: timeout·응답 오류 검증·유한 재연결·토큰 경로 고정. U: 최초 인증/거부/재실행 조작 | 성공/인증 거부/만료 토큰/서버 종료 테스트. 실패 안내 후 제어 복귀. 토큰 값 로그 노출 0회, 무한 재인증 없음. |
| **V03 메인 표현 연결** — 현재 호출되지 않는 표정을 실제 발화와 연결 | P1 / V02,P05,P07 / 1–2h / 중간 | C: 선택형 표현 작업, 발화 시작/끝 연결, 중립 복귀/종료 정리. U: 표정 전환 확인 | 연속 두 발화에서 이전 표정 작업이 새 표정을 덮지 않음. TTS 실패·VTS 불통 시 텍스트 대화 지속. VTS가 꺼져 있어도 앱 시작 가능. |
| **V04 상황별 감정 10사례** — 사용자 감정의 단순 복사를 줄이는 기본 반응 규칙 | P1 / V03,Q05 / 1–2h / 중간 | C: 위로/축하/일상 상황 매핑과 정상 기본값, TTS 강도 과장 방지. U: 표현 적절성 판정 | 10사례에 선택 감정/기대 이유 기록, 사용자 판정 9/10 이상 제안. 불확실한 경우 중립. 별도 감정 LLM을 필수로 추가하지 않음. |
| **V05 실제 표정·끊김 검수** — 자동 요청 성공을 화면 반응과 구분 | P1 / V04 / 0.5–1.5h / 높음 | C: 재연결/실패 시나리오 안내와 결과 기록. U: 실제 표정 관찰, VTS 종료/재실행 | D06 통과. 3개 표정 구분·중립 복귀 확인. VTS를 끈 뒤 다음 키보드 입력 1건 정상. |

### M — 저장과 기본 기억

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **M01 로컬 대화 저장** — 세션 종료 때 사라지는 대화를 보존 | P1 / P05,P06 / 1–2h / 중간 | C: Python 기본 SQLite로 세션/턴·입력/응답·실패 상태 저장, 비공개 DB 경로 제외. U: 저장 위치 안내 확인 | 정상 2턴 후 DB 재열기 내용 일치. 자막은 일본어 본문과 별도 필드. 잘못된 AI 응답을 성공 대화로 기록하지 않음. 개인 DB를 테스트 fixture에 복사하지 않음. |
| **M02 저장 실패·재시작 복구** — 파일 쓰기 실패 때문에 대화가 끊기지 않도록 보호 | P1 / M01 / 1–2h / 중간 | C: 트랜잭션·잠금/읽기전용/손상 안내, 메모리 세션 대체. U: 재시작 조작 | 쓰기 실패를 주입해 현재 대화 지속, 저장되지 않았음을 표시. 종료 후 마지막 완료 턴 복구. 손상 원본을 자동 삭제/덮어쓰기 하지 않음. |
| **M03 명시적 기억 등록** — 복잡한 RAG 없이 재사용할 사실을 저장 | P1 / M01 / 1–2h / 중간 | C: 기억 명령/ID·출처·시간 저장, 최근 문맥과 분리. U: 검수용 사실 3개 지정 | 지정한 사실만 저장, 같은 명령 중복 처리. 일반 대화를 임의 사실로 확정하지 않음. 사실 3개 등록/조회 성공. |
| **M04 기억 조회·대화 주입** — 저장을 실제 다음 대화에 연결 | P1 / M03,Q05 / 1–2h / 중간 | C: 적은 수의 관련 기억을 길이 제한 내 조회·주입, JP 대화 지시와 데이터 구분. U: 재시작 후 사실 질문 | 재시작 후 3사실 질문 3/3 반영. 없는 정보는 생성하지 않게 평가. 기억에 ‘이전 지시 무시’가 포함돼도 역할/언어 정책을 덮어쓰지 않는 회귀 사례 포함. |
| **M05 기억 수정·잊기** — 잘못 저장한 사실을 사용자가 통제 | P1 / M03,M04 / 1–2h / 중간 | C: ID 조회/수정/잊기·최근 대화 초기화의 의미 분리. U: 검수용 기억 수정/잊기 조작 | 수정 전 사실을 계속 사용하지 않음. 잊은 사실은 기억 조회에서 제외, 관련 세션 문맥 영향도 제거/초기화 정책대로 처리. 개인 데이터 일괄 삭제를 검수 명령으로 쓰지 않음. |
| **M06 기억·저장 사용자 검수** — 영속성과 의미 활용을 함께 확인 | P1 / M02,M04,M05 / 0.5–1.5h / 높음 | C: 전용 검수 DB로 시나리오와 결과표. U: 앱 재시작/질문/수정·잊기 확인 | D07 통과. 세션 A 등록→재시작 B 활용→수정/잊기→재시작 C 반영. 사용자 원본 DB/대화는 보존. |

### R — 전체 검수와 인수

| ID·작업명 / 필요한 이유·결과물 | 우선·선행·예상시간·불확실성 | Codex / 사용자 | 완료 조건·구체 검수 |
| --- | --- | --- | --- |
| **R01 실패 시나리오 통합** — 개별 모듈 통과를 전체 복구 보장으로 연결 | P0 / Q07,S04,V03,M02 / 1–2h / 중간 | C: 각 외부 경계에 실패를 주입하는 통합 테스트. U: 별도 작업 없음 | D08의 각 실패 뒤 정상 1턴 성공. 실패 응답/TTS의 downstream 사용 0회, 유한 재시도, 저장 상태 일치. 기존 53개와 새 회귀 모두 통과. |
| **R02 실행 구성·SSD 안내** — 실행 위치가 달라도 설정을 혼동하지 않도록 사용자 안내 | P1 / P03,S05,V05,M06 / 0.5–1.5h / 중간 | C: 주 장비 안내와 선택형 서버 URL/SSD 경로 예시, README 갱신. U: 실제 사용하는 경로 확인 | D01 통과. 없는 SSD/다른 cwd/서버 주소 오류를 구분. 두 번째 장비는 실제 검수하지 않았다면 ‘미검수’로 표시. 모델·토큰은 예시/배포물에 미포함. |
| **R03 전체 10턴 시연** — 입력부터 기억까지 사용자가 한 흐름으로 확인 | P0 / R01,R02 / 1–2h / 높음 | C: 시연 입력/단계 시간/결함 표 작성. U: 키보드·음성, 청취·표정·자막 확인 | D02–D07 통합 통과. 목소리/자막 문장 끝 일치. 실패한 기준마다 새 결함 ID와 재현 명령 부여, 기능 연결만으로 완료 처리하지 않음. |
| **R04 재시작·30턴 지속** — 짧은 데모에서 안 보이는 상태 누적 확인 | P0 / R03 / 1–2h / 높음 | C: 30턴 기록의 history 크기·자원·저장·표정 작업 누적 확인. U: 재시작 2회와 중간 서비스 중단 조작 | D09의 무처리 예외 0회, history/녹음/표정 작업 누적 없음. 장애 후 대화 복구. 측정된 메모리 증가가 있으면 원인별 결함으로 재산정. |
| **R05 최종 인수 기록** — 실제 완료 범위를 설명하는 실행 안내·검수표 | P0 / R04 및 차단 결함 해결 / 0.5–1.5h / 중간 | C: D01–D09 증거 연결, 제한·복구 절차·개인 파일 제외 점검. U: 최종 말투/음질/사용 흐름 승인 | 미검수 필수 항목/차단 결함 0개. 동작 장비·모델 설정·실행 명령 명시. 공개·push·모델 배포는 이 카드에 포함하지 않음. |

Q02 이후 속도 목표는 ‘입력 확정→첫 음성’, ‘자막 표시’, ‘전체 턴’을 따로 제안한다. 현재 n=1의 21.225초만으로 주 장비의 지속 가능한 p95나 음성 응답 목표를 확정할 수 없다. Q07에서 반드시 수치 목표와 그 근거를 채운다.

## 8. 일정과 범위 판단

2026-09-07부터 12-31까지 날짜 차이는 **115일, 약 16.4주**다. 가용 시간이 미정이므로 확정 완료일을 계산할 수 없다.
작업 카드 36개의 합은 **32.5–68.5 집중시간**, 통합 결함·환경 차이에 대한 **30% 예비 시간**을 더하면 약 **42–89시간**이다. 이는 초기 추정이고 상한 보장이 아니다. 모델 재학습, 신규 Live2D 제작, 하드웨어 구입은 포함하지 않는다.

| 가정한 시간 — 사용자 약속 아님 | 마감까지 산술 용량 | 현재 추정과 비교 |
| --- | --- | --- |
| 주 2시간 | 약 33시간 | 예비 시간 포함 최소 추정보다 작음. 구현 깊이 축소와 재산정 필요. |
| 주 4시간 | 약 66시간 | 추정 구간 안. 환경/품질 문제가 적어야 가능하며 완료 보장 불가. |
| 주 6시간 | 약 99시간 | 현재 예비 포함 상한보다 큼. 실제 확보 여부와 장비 준비에 달림. |

다음 날짜는 **역산한 점검 문턱**이며 일정 약속이 아니다. 사용자 시간이 정해지고 P01–P04 실제 소요가 쌓이면 수정한다.

| 점검 시점 | 남겨야 할 결과물 | 미달 시 선택 |
| --- | --- | --- |
| 9월 말 | 실행 환경 확정, SBV2 3문장, 텍스트 실패 복구, 20문장 기준 측정 | 새 기능보다 환경/음성 차단 문제 우선. 다른 실행 구성을 검토하되 목표 장비를 임의 변경하지 않음. |
| 10월 말 | 번역·응답 기본 검수, PTT 음성 입력 연결, VTS 기본 매핑 | 상시 청취/세밀한 표정은 후속 유지. 서버를 못 쓰면 저장/기억의 오프라인 작업을 당겨 진행. |
| 11월 말 | 표정·저장·기억까지 연결된 전체 시연 후보 | 한 주 실행 구성, 3개 기본 표정, 명시적 기억 방식으로 깊이 축소 제안. 필수 기능 누락은 완료라고 하지 않음. |
| 12월 1–14일 | 실패 상황·30턴/재시작 회귀 검수 | 결함 수정과 재검수 우선, 관련 없는 개선 중지. |
| 12월 15–31일 | 사용자 청취/마이크/Live2D 최종 검수, 실행 안내 확정 | 차단 문제가 남으면 남은 시간·미달 기능을 제시하고 범위/마감 선택을 요청. 12-31을 임의로 연장하지 않음. |

진행 순서: `P01 → P02 → P03 → P04`, 나머지 P 작업과 Q 검수 → S 음성 입력 → V 표현 → M 기억 → R 통합 검수.
M01/M02는 기존 텍스트 흐름이 안정되면 외부 장비 대기 시간에 먼저 진행해도 된다. 이유는 저장 검수가 서버 없이 가능하고 이후 음성 입력도 같은 대화 저장 경로를 사용하기 때문이다.

실제 2주 작업량으로 주당 가용 시간 W를 정한다. 예상 잔여 주수는 `예비 포함 잔여 집중시간 / W`의 범위로 계산하고 시험 기간/모델 대기 기간을 따로 표시한다. W=미정 또는 0이면 예상 완료일도 미정이다. 지연을 사용자의 가용 시간을 늘리는 방식으로 해소하지 않는다.

## 9. 사용자 환경에서 확인할 명령

이번 Mac에서 가능한 기본 확인(프로젝트 루트에서 실행):

```bash
.venv/bin/python -c 'import sys, pathlib; print(sys.executable); print(pathlib.Path.cwd())'
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python main.py
```

지금 `main.py`는 첫 Enter가 필요하다. 종료하려면 `종료`를 입력한다. **SBV2 단독 합성과 앱 TTS 모듈 연결은 12절에서 검증했지만, 이 명령의 전체 대화·재생 흐름은 아직 검수하지 않았다.**

데스크톱을 첫 검수 대상으로 선택한다면 프로젝트 루트 PowerShell에서 다음 읽기 전용 확인을 한다. 경로가 없다면 임의 환경을 새로 설치하지 말고 그 오류를 기록한다.

```powershell
.\.venv\Scripts\python.exe -c "import sys; from pathlib import Path; print(sys.executable); print(Path.cwd()); import llm, tts; print(llm.__file__); print(tts.__file__)"
curl.exe --max-time 5 http://127.0.0.1:11434/api/tags
curl.exe --max-time 5 -i http://127.0.0.1:5000/models/info
```

Ollama는 HTTP 200 JSON과 설정 모델 존재가 필요하다. SBV2는 HTTP 200만으로 충분하지 않으며 모델 정보 JSON과 지정 모델/화자/스타일이 일치해야 한다. `AirTunes`, HTML, binary plist, 403이면 요청 대상부터 확인한다. 실제 서버가 다른 장비/포트라면 확정한 주소로 바꾼다. API 키·토큰·학습 파일을 보내지 않는다.

SBV2 서버를 직접 실행할 때의 경로 확인 예시(장비·환경을 확정한 후 사용):

```text
작업 디렉터리 = 선택한 프로젝트/SBV2-KR-master
<SBV2 환경의 Python 절대 경로> -c "import sys, pathlib, style_bert_vits2; print(sys.executable); print(pathlib.Path.cwd()); print(style_bert_vits2.__file__)"
<SBV2 환경의 Python 절대 경로> server_fastapi.py --dir "<개인 model_assets 절대 경로>"
```

이 서버 코드에는 `--port` 옵션이 없다. 사용할 포트는 **해당 트리의 `config.yml`의 `server.port`**와 앱 `SBV2_API_URL`을 맞춘다. 사용 가능한 포트를 확인한 뒤 변경한다. `--dir`은 모델들을 담은 상위 폴더다. Mac CPU 시험은 `--cpu`를 사용할 수 있으나 속도·전체 로드는 미검수다.

확인 결과: 시작 로그의 worker 시작/예외 여부, 모델 목록, listen 주소, `/models/info` 일치. worker 연결 실패가 다시 나타나면 해당 시도의 Python·cwd·모듈 경로와 마지막 오류 부분을 함께 기록한다. 과거 백업 폴더를 실행한 기록은 이번 오류 원인으로 재사용하지 않는다.

서버 확인 후 개인 모델의 실제 합성/청취는 P04에서 실행한다. 현재 `tts.py` 단독 실행은 `output.wav`를 덮어쓰므로 진단 전 기존 파일을 덮어쓰는 명령을 사용자에게 요구하지 않는다. P04에서 별도 검수 파일로 생성한다.

## 10. 보호 조치와 변경 기록

| 작업 | 구현 | 검수 | 이번 변경 |
| --- | --- | --- | --- |
| A00 현재 상태 조사·계획 | 완료 | E01–E14 범위 완료 | 이 문서 생성. 전체 시스템 실검수는 미완료. |
| A01 비공개 파일 제외 | 완료 | 현재 존재한 토큰·설정·데이터와 모델의 ignore 확인 | 기존 `.gitignore` 끝에 토큰/설정/학습 데이터·입력 디렉터리 규칙 추가. 기존 파일/내용 보존. |

문서 검증: 작업 ID 36개와 중복 없음, 시간 합계 32.5–68.5h, 선행 작업 ID와 README의 계획 링크를 확인했다. 이번 수정 파일은 `PROJECT_PLAN.md`, README의 계획 링크 안내, `.gitignore`다. 앱 코드와 기존 테스트는 이번에 수정하지 않았다.

`.gitignore` 적용 전에 해당 토큰/설정/데이터/모델 경로가 현재 Git 추적 목록에 없음을 확인했다. 이것은 전체 Git 과거 이력의 비밀정보 감사를 뜻하지 않는다. 모델 원본·데이터·인증정보를 이 문서에 복사하거나 공개하지 않는다.

## 11. 다음 갱신 규칙

각 작업 후 카드의 구현/검수 상태와 실제 집중시간을 갱신하고 다음 형태로 증거를 추가한다.

```text
날짜 / 작업 ID / 구현 상태 / 실환경 검수 상태
실행 장비·Python·코드 기준 / 실행 명령 / 입력 사례 ID
관측 결과·실패 이유 / 실제 집중시간 / 미확인 사항
영향받는 후속 ID / 잔여 시간 재추정 / 다음 행동 한 가지
```

오류 로그만 받더라도 해당 ID에 연결해 원인과 후속 영향을 먼저 판단한다. 같은 코드·환경에서 완료한 검수는 관련 변경/실패가 없으면 반복하지 않는다. 임시 조치에는 제거 조건을 붙이고 장비가 준비되면 다시 평가한다.

**현재 다음 행동:** 실행 기준 고정 작업은 완료됐다. 이후 실행에는 README의 키 숨김 입력을 포함한 절차를 사용한다. 이번 검수의 추가 입력·청취·재실행은 필요 없다. 후속 기능 개발은 별도 범위로 진행한다.

## 12. SBV2 복구·실제 합성 검증 (2026-09-07 16:06–16:09 KST)

사용자 승인 범위: `bert_models.py` 두 dtype 인자 수정, 앱의 5001 연결 주소와 CosyVoice fallback 비활성화, 서버 기동부터 실제 합성까지 순차 검증. 패키지 변경·모델 교체·커밋·푸시는 하지 않았다.

### 원인과 변경 경위

- 실제 Mac Python은 루트 `.venv/bin/python`, Python 3.11.0, transformers 4.51.3, torch 2.5.1이다. `SBV2-KR-master/style_bert_vits2/nlp/bert_models.py`가 실제 import됨을 확인했다.
- 같은 `.venv` 안의 Windows형 `Lib/site-packages`에는 transformers 5.14.1과 Windows용 torch가 존재하고, Mac형 `lib/python3.11/site-packages`에는 4.51.3이 존재한다. 이번 Mac 실행은 후자를 사용한다. 과거 정상 실행이 Windows 5.14.1이었다는 것은 가능한 설명이며, 정상 실행 기록이 없어 확정하지 않는다.
- 해당 dtype 코드와 5.x 관련 주석은 초기 커밋 `bd1a680`(2026-08-09)부터 존재했다. 수정 전 파일은 HEAD와 바이트 단위로 같았다. 현재 로컬 Git 이력은 이 커밋 하나여서 누가 언제 그 코드를 작성했는지, 이전 설치가 무엇이었는지 단정할 수 없다.
- transformers 4.51.3에서 `dtype`가 Deberta 생성자로 넘어가 TypeError가 났다. 사전 메모리 시험에서 `torch_dtype="float32"`로 일본어·영어 양쪽 작은 모델의 float32 로딩·추론이 성공했다. 이를 근거로 실제 두 호출 인자만 교체했다.
- 사용자가 바꾼 `config.yml`의 server.port=5001은 유지했다. 앱 `tts_config.json`의 URL을 5000→5001, `fallback_to_cosyvoice`를 true→false로 변경했다. 모델/화자/스타일/timeout 값은 유지했다.

### 실행 순서와 관측 결과

| 증거 | 실행·조건 | 결과·한계 |
| --- | --- | --- |
| SB01 | 승인된 두 파일 수정 후 기존 `unittest discover -s tests -q` | 53개 통과, 0.024초. 수정한 bert_models.py의 `git diff --check` 통과. |
| SB02 | SBV2 폴더에서 `../.venv/bin/python -B -u server_fastapi.py` | 도구의 sandbox에서는 호스트 이름 해석 실패 후 worker timeout. 승인된 sandbox 밖 실행에서는 worker 시작 성공. 이 제약을 사용자 일반 터미널의 새 코드 결함으로 취급하지 않음. |
| SB03 | 같은 명령으로 host 실행 | 16:07:00 worker·JP BERT·토크나이저 시작, 5001 listen. 기록 당시 서버 PID 5209, worker PID 5265/7861. CPU 선택. 해당 PID는 이후 재시작 시 달라질 수 있음. |
| SB04 | GET `/docs`, `/openapi.json`, `/models/info` | `/docs` 200 text/html+Swagger UI. SBV2 API 경로 확인. 8개 모델 노출, 지정 모델 id=1, 체크포인트/Elaina 화자/Neutral 일치. 폴더 9개와 실제 노출 모델 8개는 다른 집계다. |
| SB05 | POST `/g2p?text=こんにちは。` | HTTP 500, server_fastapi.py:291에서 `g2kata_tone` 미정의 NameError. `normalize_text`도 import 없음. 이 엔드포인트는 `/voice`에서 호출되지 않아 뒤의 합성 시험은 별도로 진행. 이 파일은 이번에 수정하지 않음. |
| SB06 | 루트 실제 `tts.speak()`를 통한 3회 `/voice` | 전부 성공. 서버 로그에 지정 JP-Extra 체크포인트 로드와 3회 Audio data generated successfully 확인. 출력 경로만 시험 프로세스 안에서 개별 검수 WAV로 지정, 네트워크·합성은 mock하지 않음. |
| SB07 | SciPy로 WAV 파싱·파형 검사 | 3개 모두 44,100Hz, int16, 프레임 존재, 유한값·비무음 확인. 사람의 음질·발음·문장 누락 판정을 대신하지 않음. |
| SB08 | 실행 전후 SHA-256 비교 | 기존 output.wav, 서버 config.yml, 지정 251,187,844-byte 개인 체크포인트 모두 동일. 기존 출력/모델 원본 보존. |

검수 파일과 수치는 [results.json](artifacts/sbv2_validation/20260907-160926/results.json)에 기록했다.

| 파일 | 입력 | 요청→저장 시간 | 오디오 길이 |
| --- | --- | --- | --- |
| [01-greeting.wav](artifacts/sbv2_validation/20260907-160926/01-greeting.wav) | こんにちは。今日はいい天気ですね。 | 7.725초 | 2.856초 |
| [02-question.wav](artifacts/sbv2_validation/20260907-160926/02-question.wav) | 今日は学校で何をしましたか？ | 0.953초 | 2.380초 |
| [03-happy.wav](artifacts/sbv2_validation/20260907-160926/03-happy.wav) | 会えてうれしいです。一緒にゆっくり話しましょう。 | 1.219초 | 3.019초 |

첫 두 건은 speed=1.0/normal, 세 번째는 speed=1.2/happy assist_text다. 모두 JP/Neutral을 사용했다. 첫 요청에는 체크포인트 로딩이 포함된다. 서로 다른 문장 3건의 시간이며 평균 지연·p95·전체 앱 응답속도·음성 품질 목표로 일반화하지 않는다.

### 작업 상태와 후속 영향

| 작업 | 구현·기계 검증 | 남은 실검수 |
| --- | --- | --- |
| P02 | 현재 로컬 설정의 SBV2 지정·CosyVoice fallback 비활성화 완료 | 설정 누락 때의 코드 기본값 및 잘못된 JSON/자료형 처리 개선은 아직 미구현. 카드 전체 완료 아님. |
| P03 | 현재 Mac의 BERT 호환성 수정, 5001 기동·서버 식별·모델 일치·실제 합성 경로 검증 완료 | `/g2p` 보조 API는 별도 import 결함으로 미통과. 원래 카드의 모든 기준을 통과한 것으로 표시하지 않음. |
| P04 | **완료**: 개인 모델 음성 3건 생성·파일 검증 완료 | **사용자 검수 완료**: “3가지 다 정상이야 이상한점 없는거 같아.” 세 파일 모두 이상 없음으로 접수. |
| P05 및 D02 | 13절에서 기존 메인의 한국어 입력→번역→대화→자막→재생 1턴 사전 검수 통과 | P05 함수 분리 구현은 대기. D02의 10턴·여러 입력 사례는 미검수. |

핵심 SBV2 기동 차단 문제는 해소됐다. `/g2p` 결함은 P03의 별도 소규모 수정·재검수 대상으로 남기며, 정상 `/voice` 경로와 구분한다. 사용자 P04 청취 판정을 정상으로 접수했고 전체 앱 1턴 사전 검수도 통과했다. 연말 마감과 사용자 가용 시간은 변경하지 않는다. P04가 완료돼 남은 카드의 초기 추정 합은 32.0–67.0h, 동일 30% 예비 적용 시 약 42–87h다. 부분 완료분은 계측한 실제 집중시간이 없어 추가 차감하지 않았다. 가용 시간 미정으로 완료일은 여전히 확정하지 않는다.

재시작 명령(서버가 이미 5001에서 실행 중이면 중복 실행하지 않음):

```bash
cd /Users/eonpisa_1017/Desktop/AI_Waifu_System/SBV2-KR-master
/Users/eonpisa_1017/Desktop/AI_Waifu_System/.venv/bin/python -B -u server_fastapi.py
```

이 합성 시험에서는 자동 재생을 실행하지 않았다. 이후 사용자가 위 세 WAV 파일을 직접 청취하고 모두 정상이라고 확인했다.

## 13. P04 사용자 승인과 메인 1턴 사전 검수 (2026-09-07 16:26 KST)

사용자 피드백: **“3가지 다 정상이야 이상한점 없는거 같아.”** P04의 세 음성은 생성 검사와 사용자 청취 모두 완료했다. 이 판정을 새로운 문장 전체의 발음·음질 보장으로 일반화하지 않는다.

다음 단계로 기존 루트 `main.py`를 `runpy.run_path(..., run_name="__main__")`로 실행했다. 표준 입력은 시작 Enter → `안녕. 짧게 인사해 줘.` → `종료` 순서의 문자열 스트림으로 공급했다. 실제 손으로 키보드를 조작한 검수는 아니다.

이 시험에서 앱 코드는 수정하지 않았다. 기존 output.wav를 보존하려고 시험 프로세스 안에서 `tts.OUTPUT_PATH`를 검수 폴더로 설정하고 작업 디렉터리도 그 폴더로 바꿨다. 루트 모듈 경로를 명시하고 단계별 함수에는 시간 기록 래퍼만 씌웠다. 번역·LLM·SBV2·`audio_playback.play_wav()`는 실제 구현을 호출했으며 응답·재생 성공값은 mock하지 않았다. 이 출력 격리 시험을 운영 코드의 일반적인 cwd/경로 문제 해결로 취급하지 않는다.

| 단계 | 관측 결과 | 소요 |
| --- | --- | --- |
| 한국어 입력 보정·일본어 번역 | status=ok. 원문 유지, `こんにちは。短く挨拶してね。` | 11.907초 |
| 일본어 응답 검증·재생성 | 첫 응답 거부 후 재생성 1회. 최종 `こんにちは。今日も元気？少しだけ近づくね。` | 4.059초 |
| 한국어 자막 | Gemini 키 없음→Qwen. `안녕하세요. 오늘도 기분이 좋으세요? 조금 더 가까이 다가갈게요.` | 2.724초 |
| SBV2 합성 | 현재 5001 설정의 지정 모델 사용, speak=True. 이 표본은 80자 제한에 잘리지 않음 | 5.019초 |
| 실제 WAV 재생 | `play_wav()`→macOS `afplay`, 반환값 True | 5.135초 |
| 다음 입력·종료 | 다시 입력을 받고 `종료` 처리. history 역할 system/user/assistant | 전체 **28.876초** |

최종 어시스턴트 history에는 검증된 일본어 응답이 들어갔다. 원본 output.wav의 SHA-256이 시험 전후 동일함을 확인했다. 실제 재생 명령 성공을 사용자 청취 판정으로 대신하지 않는다. 입력 확정부터 합성 완료까지 단계 합은 약 23.709초이고 전체 시간에는 재생·진단 출력도 포함된다. n=1의 결과로, 평균 지연/p95나 개선 목표는 아직 정하지 않는다.

증거: [console.log](artifacts/main_validation/20260907-162625/console.log), [results.json](artifacts/main_validation/20260907-162625/results.json), [생성 WAV](artifacts/main_validation/20260907-162625/output.wav).

이번 갱신에서 변경한 프로젝트 문서는 이 계획 파일뿐이다. 새 검수 폴더에 로그·결과·WAV를 생성했다. 애플리케이션 코드·패키지·모델은 추가 수정하지 않았다. 정상 1턴을 통과했으므로 P05/P06에서 원래 동작을 유지할 기준 결과로 사용한다. 오류 복구·10턴 시연·말투 품질 검수는 아직 남아 있다.

## 14. Q06 번역기 채택 로그·키 비노출 수정 (2026-09-07)

사용자 승인: 앞서 제안한 최소 수정 진행. 검수 실행기는 가능하면 임시 방식으로 사용하고, 번역 로그에는 채택된 번역기·모델·폴백 사유만 남긴다. API 키·요청 헤더·응답 원문 전체를 기록하지 않는다. 프로젝트에 새 파일은 추가하지 않았으며 패키지·모델 변경, 커밋·푸시는 하지 않았다.

### 구현과 현재 검증

| 대상 | 변경·증거 | 한계 |
| --- | --- | --- |
| `translator.py`, `logging_setup.py` | 검증 통과 후 `JA->KO provider=gemini/qwen model=... fallback_reason=...` 한 건 기록. Gemini 채택은 기본 INFO에서 보이고, 폴백은 WARNING. 둘 다 실패하면 provider/model=none이며 두 경로의 실패 사유 코드를 기록. 요청 메타데이터·번역 원문 디버그 로그 제거. 검증 사유에 붙던 실제 단어/문자도 로그에서 제외. | 채택 로그는 언어·형식 검증 통과의 증거이며 의미·맞춤법의 보증은 아님. |
| `gemini_translator.py` | 기존 모델·REST 경로·요청 형식·timeout 유지. 예외 문자열 출력 대신 timeout/통신/응답 형식/HTTP 상태 코드를 반환하고 최종 채택 로그에 포함. | 실제 키 권한·할당량·API 요청 성공은 미검수. |
| `japanese_response.py` | 재생성 시 거부된 원문 출력과 통신 예외 문자열 출력만 제거. 검증 사유와 재생성 경고, 실제 생성·검증 동작 유지. | 응답 품질 개선 작업은 아님. |
| 프로젝트 테스트 | 수정 전 **53개 / 0.026초 / OK**. 수정 후 **62개 / 0.031초 / OK**. 기존 53개 중 원문 로그를 요구하던 검증은 이번 비노출 조건에 맞춰 갱신했으며, 새 테스트는 9개. | 실제 네트워크 요청은 mock. 이전 53개를 전혀 수정하지 않고 그대로 통과한 것으로 표현하지 않음. |
| 새 회귀 사례 | Gemini 채택, 잘못된 Gemini→Qwen, HTTP 403/timeout/통신 오류, 잘못된 응답·JSON, Qwen도 실패, 한국어 입력 원문·예외 비노출, 기본 모드 로그 중복 방지, 일본어 생성 예외 비노출. 가짜 키를 넣은 예외에서 키/헤더/본문 미출력 확인. | 실키는 사용·기록하지 않음. |
| 임시 단건 실행기 | 프로젝트 밖 `/private/tmp/ai-waifu-gemini-a9rsi8s3/check_gemini.py`. getpass 숨김 입력, 비TTY·숨김 입력 불가 시 중단, Google 기본 API 주소 확인, 실제 `translator.japanese_to_korean()` 호출. 자막은 화면에만 표시하고 로그/응답 파일은 만들지 않음. | 임시 파일은 재부팅/OS 정리로 없어질 수 있으며 프로젝트 필수 의존성이 아님. |
| 임시 실행기 모의 검증 | **5개 / 0.014초 / OK**. 성공·키 정리, Qwen 폴백을 PASS로 오판하지 않음, 비TTY 차단, 입력 에코 차단, 예외 내용 비노출. | 프로젝트 테스트 62개와 별도 집계. 실제 Gemini 연결을 통과한 것은 아님. |
| 보존·정적 검사 | 수정 대상 `git diff --check` 통과. 실행 전후 SHA-256으로 `main.py`, `tts.py`, `tts_config.json`, SBV2 `config.yml`, `bert_models.py`, 기존 `output.wav` 동일 확인. | 이번에 음성 합성을 반복하지 않았으며, 이전 사용자 청취 결과를 유지함. |

사용자가 Gemini API 키 발급 완료를 알렸다. 키 값은 전달받지 않았다. 현재 도구 프로세스의 `GEMINI_API_KEY`는 미설정이다. 사용자의 별도 터미널 환경까지 미설정이라고 단정하지 않는다. 임시 단건 실행기의 존재와 문법을 다시 확인했으며, 실제 요청은 사용자 숨김 입력 후 진행한다. 키는 채팅·코드·설정 파일·로그에 저장하지 않고, 단건 실행 프로세스 종료 시 환경에서 제거한다. 입력한 키는 이후 새 실행에 자동 저장되지 않는다.

### 다음 검수와 작업 상태

| ID | 상태·담당·예상 집중시간 | 완료 조건 |
| --- | --- | --- |
| Q06-A 채택 로그·비노출 회귀 | **구현·자동 검증 완료**, Codex | 위 프로젝트 62개와 임시 실행기 5개 통과. |
| Q06-B Gemini 단건 실연결 | **완료**, 사용자 실행 결과와 Codex 원문·자막 대조로 확인. 상세는 15절. | provider=gemini, 지정 모델, fallback_reason=none, PASS. 인사·학교 질문의 뜻 보존. |
| Q06-C 기존 메인 4턴 검수 | **4/4턴 기계 검수·직접 Gemini 채택 로그 확인 완료**, 전체 대화 품질 미통과·청취 판정 대기. 상세 23절. | 연결 성공과 문맥·말투·음질을 구분. 마지막 턴 사실 회상 실패와 위로 말투 문제를 Q05로 연결. 원문 전체 로그 파일은 만들지 않음. |

위 시간은 다음 작업의 집중시간 추정이며 API 지연 측정값이나 사용자의 확보된 작업 시간이 아니다. 연말 마감·가용 시간 미정은 유지한다. 전체 잔여량의 이전 초기 추정 32–67h(+30% 예비 약 42–87h)는 실측 집중시간이 없어 이번에 임의 차감하지 않는다. 4턴 검수는 D02의 10턴 시연 및 P06 장애 복구 전체 검수와 구분한다.

일반 터미널에서 실행할 다음 명령:

```bash
/Users/eonpisa_1017/Desktop/AI_Waifu_System/.venv/bin/python -B /private/tmp/ai-waifu-gemini-a9rsi8s3/check_gemini.py
```

검수 문장은 코드에 고정된 일반적인 인사·학교 질문이다. Gemini 실패 시 기존 Qwen 경로를 실제로 실행하며, Qwen 자막이 나와도 단건 Gemini 검수는 FAIL로 표시한다. 사용자에게 받을 결과는 provider 로그, 표시된 자막, 단건 소요 시간, PASS/FAIL뿐이다. 실제 연결이 확인되면 이 절에 실환경 결과를 추가한다. 임시 실행기는 단건 검수 종료 후 더 이상 사용하지 않으며, 반복 검수가 필요해지기 전에는 프로젝트 파일로 승격하지 않는다.

## 15. Q06-B 실제 Gemini 연결 결과와 4턴 검수 준비 (2026-09-07)

**증거 출처는 사용자가 임시 단건 실행기를 실행하고 전달한 결과다.** Codex 도구에서 실키를 받아 실행한 것으로 표시하지 않는다. 키 값은 전달받지 않았다.

| 항목 | 관측 결과 |
| --- | --- |
| 키 | 사용자 프로세스에서 설정됨, 값 비공개 |
| 실제 채택 로그 | `provider=gemini model=gemini-3.5-flash-lite fallback_reason=none` |
| 자막·의미 | 인사와 오늘 학교에서 한 일을 묻는 자연스러운 한국어 자막. 고정 원문과 의미 일치를 Codex가 대조 확인. 혼합 언어·설명문·문장 깨짐 없음. |
| 단건 결과 | PASS |
| 소요 시간 | **1.084초**, 번역 호출·검증 구간, 합성·재생 제외. n=1이며 평균/p95/장비 전체 응답속도로 일반화하지 않음. |
| 남은 검수 | 실제 메인의 연속 대화·문맥·긴 자막 품질·음성 합성·청취·다음 입력 복귀 |

원문 전체를 추가 로그 파일로 저장하지 않았다. 이 고정 문장 한 건의 통과를 모든 번역 품질이나 서비스 안정성의 보증으로 사용하지 않는다. 단건 연결의 키 준비 장애가 해소됐으며 연말 마감과 사용자 가용 시간 미정은 유지한다.

### 임시 연속 검수 방식

프로젝트 밖 `/private/tmp/ai-waifu-gemini-a9rsi8s3/check_conversation.py`를 준비했다. 기존 루트 `main.py`를 `runpy`로 실행하며, 원래 번역·LLM·합성·재생 함수를 호출하는 시간 계측 래퍼만 프로세스 안에 적용한다. 실검수에서 API 응답이나 재생 결과를 모의 값으로 대체하지 않는다. 시작 Enter만 자동 처리하고 한국어 대화는 사용자가 직접 입력한다.

- 사용자에게 한 턴씩 입력을 안내한다. 매 턴 다음 입력 함수에 도달하는 순간 계측을 끝내므로 결과 전달·타이핑 대기 시간은 제외한다. 4턴 뒤 메인의 `종료` 명령으로 정상 종료한다.
- 입력 보정 여부, 입력 번역, JP 검증·재생성 횟수, 자막 검증, 합성·WAV 파싱·재생 성공, 80자 제한 잘림, 요청에 포함된 이전 AI 답변 개수와 단계별/전체 시간을 **화면에 표시**한다. 의미·발음·음질은 별도로 판정한다.
- Gemini 대신 Qwen이 채택되거나 음성이 잘리거나 단계가 실패하면 기계 검수를 PASS로 표시하지 않는다. 앱 예외로 종료되면 다음 입력 복귀=NO이며, 임시 래퍼의 예외 안내를 앱 오류 복구 성공으로 취급하지 않는다.
- 키는 숨김 입력·메모리에서만 사용하며 종료 시 제거한다. 로그에는 번역기 채택 이벤트만 통과시키고, SBV2 등의 오류 응답 본문도 임시 실행기에서 출력하지 않는다. 앱 화면의 응답·자막을 로그 파일로 복제하지 않는다.
- WAV는 새 `/private/tmp/ai-waifu-conversation-*` 폴더의 턴별 하위 폴더에 저장한다. 시험 프로세스의 cwd와 `tts.OUTPUT_PATH`를 같이 지정해 기존 `output.wav`를 보존한다. 이는 운영 코드의 일반적인 경로 문제 수정이 아니다.
- 실행 종료 후 임시 실행기는 프로젝트 필수 파일로 유지하지 않는다. 원본 앱·SBV2·모델·설정 변경이나 커밋·푸시는 하지 않았다.

### 준비 검증

임시 연속 검수기의 **모의 실행 7건 / 0.058초 / 통과**: 실제 메인 루프를 이용한 4턴·history 전달·시간 구간·출력 격리, 재생성 횟수/시간, 80자 잘림을 성공으로 오판하지 않음, TTS 실패 시 재생 생략·다음 턴, 앱 예외를 복귀로 오판하지 않음, Qwen 폴백을 Gemini 성공으로 오판하지 않음, 비TTY 키 입력 차단. 네트워크와 음성 재생은 이 준비 시험에서만 mock했다. 가상 시계를 사용해 사용자 입력 대기 60초가 턴별 15초 계산에 섞이지 않는지 확인했으며 이 숫자는 실제 응답 시간이 아니다.

추가 **키 입력 보호 2건 통과**: 숨김 입력 키가 실행 중 메모리에만 존재하고 종료 시 제거됨, 입력 에코 경고 시 세션 시작 차단·키 비노출. 임시 검수기 총 **9건**이며 프로젝트의 기존 **62개**와 별도 집계다. 이번에는 프로젝트 실행 코드를 바꾸지 않아 62개를 다시 실행하지 않았다.

현재 read-only 확인에서 Ollama PID 1879가 11434, Python PID 2812가 5001을 LISTEN하고 있다. 이 포트 확인은 실제 합성 성공을 대신하지 않는다. SHA-256으로 `main.py`, `tts.py`, `tts_config.json`, SBV2 `config.yml`/`bert_models.py`, 기존 `output.wav`가 준비 시험 전후 보존됐음을 확인했다.

실행 명령:

```bash
/Users/eonpisa_1017/Desktop/AI_Waifu_System/.venv/bin/python -B /private/tmp/ai-waifu-gemini-a9rsi8s3/check_conversation.py
```

첫 입력: `안녕. 오늘 학교에서 시험을 봤어. 한 문장으로 짧게 답해 줘.`

**후속 결과:** 첫 턴이 입력 번역에서 실패했다. 성공은 0/4턴이며 16절의 원인 수정 후 재개한다. 정상 인사·학교 이야기 → 감정 반응 → 가벼운 입력 보정 → 앞선 문맥 확인 순서를 유지하되 실제 응답에 따라 조정한다.

## 16. 연속 검수 첫 턴 실패와 입력 번역 완결성 오판 (2026-09-07)

### 사용자 실행 결과

| 항목 | 결과 |
| --- | --- |
| 실행 | 임시 검수 세션 `/private/tmp/ai-waifu-conversation-7yrj33xi`, 첫 입력은 15절의 세 문장 |
| 입력 번역 | 실패, **17.818초** |
| JP 응답·Gemini 자막·SBV2·재생 | 모두 미실행. 이 실패를 Gemini/SBV2 장애로 취급하지 않음. |
| 다음 입력 | **복귀 성공**, 이전 AI 답변 0개 |
| 음성 파일 | 보고된 세션 폴더에 턴 WAV 0개를 read-only 확인 |

메인의 “Ollama 번역 모델 연결을 확인” 문구는 입력 번역 함수가 None을 반환할 때 나오는 공통 안내다. 이 문구만으로 서버 연결 실패를 확정할 수 없다. 임시 실행기의 문장 잘림=NO도 TTS가 실행되지 않은 이번 턴의 정상 음성 판정을 뜻하지 않는다.

### 원인 재현과 확인 범위

도구에서 현재 Ollama에 동일한 한국어 입력을 보냈다. 네트워크가 허용된 실행에서 **두 요청 모두 정상 응답**, JSON 필드·status=ok·입력 보정 검증 통과, 보정 원문 유지. 그러나 두 시도 모두 `incomplete_translation`으로 거부됐다. 각각 3.695초/3.762초이며 사용자의 최초 17.818초와는 별도의 재현 실행이다. API 키·요청 헤더·응답 원문 전체는 출력하거나 파일에 저장하지 않고 검증 사유·문장 수만 확인했다.

직접 원인은 `translator.py:116`의 `_is_likely_incomplete()`다. 원문 문장 수는 `[.!?。！？]+`로 세고 번역문은 `[.!?]+`로 세므로 일본어 마침표 `。` 등이 누락된다. 재현에서 **원문 3문장, 일본어 문장부호 3개, 현재 함수가 센 번역 문장 수 0개**였다. 문자 길이 검사는 원문 core 길이 23이라 해당 실패에 관여하지 않았다.

완전한 3문장 일본어 테스트 자료에서도 동일하게 거부되고, `。`만 `.`으로 바꾸면 통과했다. `HEAD`의 `translator.py`에도 같은 계산식이 있어 이번 번역기 채택 로그 수정에서 새로 도입된 결함은 아니다. 변경 주체나 과거 정상 환경에 관해서는 이 사실 이상으로 추정하지 않는다. 사용자 최초 실행의 실제 응답 원문은 보관하지 않아 그 응답 자체를 소급 분석한 것은 아니며, 동일 입력·현재 코드의 실호출로 결함을 재현한 것이다.

### 최소 수정안과 사전 검증 — 파일 미적용

```diff
-    translated_sentences = len(re.findall(r"[.!?]+", translation))
+    translated_sentences = len(re.findall(r"[.!?。！？]+", translation))
```

`translator.py` 한 줄과 기존 `tests/test_translator.py`의 회귀 테스트 2개 추가만 제안한다. 완전한 일본어 다문장 번역의 `。！？`를 인식하고, 실제 문장이 빠진 번역은 계속 재시도하는지 확인한다. 모델·프롬프트·Gemini·SBV2 설정은 바꾸지 않는다.

위 한 줄을 파일 변경 없이 프로세스 메모리에만 적용해 **기존 62개+새 회귀 2개=64개 / 0.031초 / 통과**했다. 새 회귀는 일본어 문장부호를 포함한 완전한 번역이 재시도 없이 통과하는 경우와, 한 문장이 빠진 결과는 재시도 후 완전한 결과를 채택하는 경우다. 기존 한국어 자막 누락·혼합 언어 검사도 통과했다. 이는 수정안의 모의 검증이며 실제 파일 적용 후 테스트나 연속 대화 성공을 대신하지 않는다.

### 작업·일정 영향

- **Q06-D 입력 번역 문장 수 오판 수정**: P0, Q06-C 재개 전 필요. Codex가 한 줄 수정·회귀 2개 추가·자동 검증, 사용자는 범위 확인 후 임시 검수기를 재시작한다. 예상 추가 집중시간 10–20분, 불확실성 낮음. 현재 상태는 수정안 검증 완료·파일 적용 확인 대기.
- **Q06-C 연속 검수**: 첫 실패 후 다음 입력 복귀는 확인했지만 성공 0/4턴이다. 수정 후 같은 첫 문장부터 4턴 성공 검수를 시작한다. 이미 로드된 Python 함수는 디스크 수정만으로 갱신되지 않으므로 현 세션을 그대로 이어서 수정 검수하지 않는다.
- 기존 Gemini 단건·SBV2 합성/청취 결과는 유지하며 반복하지 않는다. 연말 마감과 가용 시간 미정은 그대로다. 추가 작업만 식별했으며 사용자 작업 시간을 임의로 늘리거나 완료일을 확정하지 않는다.

## 17. Q06-D 승인된 수정 적용·회귀·Ollama 실호출 검증 (2026-09-07)

사용자는 16절의 한 줄 수정과 회귀 테스트 2개 추가를 승인했고, 테스트 통과 후 검수 세션 재시작·Gemini 단건 재확인·4턴 검수를 요청했다. 따라서 이번에는 앞선 단건 통과와 별개로 새 세션의 Gemini 연결을 한 번 재확인한다.

### 적용 내용과 검증 결과

| 항목 | 관측 결과 |
| --- | --- |
| `translator.py` | `_is_likely_incomplete()`의 번역 문장부호 계산에 `。！？`를 포함하는 한 줄 수정 적용. 원문 계산과 동일한 문장부호 집합 사용. |
| `tests/test_translator.py` | 정상 일본어 다문장 번역이 1회에 통과하는 회귀 1개(문장부호 두 사례)와 한 문장이 빠진 번역은 재시도하는 회귀 1개 추가. |
| 파일 적용 후 전체 테스트 | `.venv/bin/python -B -m unittest discover -s tests -q`: **64개 / 0.018초 / OK**. 이전 프로젝트 62개에 이번 회귀 2개를 추가한 수이며 메모리 시험만의 결과가 아님. |
| 정적 검사 | 변경한 두 파일의 `git diff --check` 통과. |
| 실제 Ollama 재검수 | 동일한 세 문장 입력, `qwen3.5:9b`, 입력 보정 원문 유지, **요청 1회·입력 번역 통과·7.506초**. 응답 원문·키·헤더는 출력하거나 로그 파일에 저장하지 않음. |
| SBV2 보존 | SHA-256으로 `main.py`, `tts.py`, `tts_config.json`, SBV2 `config.yml`/`bert_models.py`, 기존 `output.wav` 동일 확인. |
| 원격·환경 변경 | 커밋·푸시·패키지 변경·모델 변경 없음. |

7.506초는 입력 번역 한 건의 새 실호출 측정치다. 사용자 최초 실패 17.818초나 별도 재현 두 건과 실행 조건을 통제한 성능 비교가 아니므로 속도 향상률로 표현하지 않는다. Gemini·음성·연속 대화 단계는 이 도구 재검수에 포함하지 않았다.

### 다음 사용자 조작

1. 현재 열린 검수 세션의 `너:`에 `종료` 입력. 이 세션의 `미완료/FAIL` 종료 요약은 이전 첫 턴 실패 결과이며 새 장애가 아니다.
2. 셸 복귀 후 임시 `check_gemini.py`를 새로 실행해 키를 숨김 입력하고 단건 채택 로그 확인.
3. 단건 통과 후 `check_conversation.py`를 새 프로세스로 실행하고 같은 첫 문장부터 4턴 검수. 각 턴을 확인하는 동안 해당 세션은 유지.

사용자에게는 위 조작을 한 번에 하나씩 안내한다. **현재 Q06-D 완료, 새 Gemini 재확인 대기, 연속 검수 성공 0/4턴**이다. 마감과 가용 시간을 변경하지 않는다.


## 18. Gemini 재확인 PASS·임시 연속 검수기 복원 (2026-09-08)

사용자 실행 결과에서 `provider=gemini model=gemini-3.5-flash-lite fallback_reason=none`, **1.104초**, PASS를 확인했다. 표시된 한국어 자막은 고정 원문의 인사·학교 질문 의미를 보존했고 혼합 언어나 설명문이 없었다. 종결어미는 이전 결과보다 격식 있다. 이 단건 시간은 합성·재생 제외이며 평균 지연으로 일반화하지 않는다. 키는 전달받지 않았다.

이후 사용자가 출력된 `INFO | translator | JA->KO ...` 줄을 셸 프롬프트에 입력해 zsh command not found가 발생했다. 이는 출력 로그를 명령으로 실행한 별도 셸 오류이며 앞선 Gemini PASS를 무효화하지 않는다. 프로젝트에서 크기 0인 `KO` 파일을 확인했다. 해당 명령의 `>` 리다이렉션으로 생겼을 가능성이 있으나 생성 이력을 직접 관측하지 않았으므로 추정으로 구분하며, 파일은 삭제하지 않았다.

이전 `/private/tmp/ai-waifu-gemini-a9rsi8s3/check_conversation.py`가 없음을 확인하고 **프로젝트 밖** `/private/tmp/ai-waifu-gemini-guqz37yp/check_conversation.py`에 연속 검수기를 복원했다. 앱 코드·설정은 수정하지 않았다. `translator.py`의 일본어 문장부호 수정이 유지된 것도 확인했다.

새 검수기는 실제 `main.py`와 번역·합성·재생 함수를 프로세스 안의 계측 래퍼로 호출한다. 키는 getpass로 받아 프로세스 환경변수에만 설정하고 종료 시 제거한다. 번역기 채택 로그만 출력하고 API 응답 본문을 로그 파일에 저장하지 않는다. 응답·자막과 검수 수치는 화면에서 확인하고 WAV만 임시 세션 폴더에 저장한다. 단계 미실행은 NO와 구분해 `미실행`으로 표시한다. 단건 재확인 스크립트도 현재 같은 임시 폴더에 존재한다.

**새 임시 연속 검수기 모의 검증 10건 통과**: 4턴 정상 흐름·history·입력 대기 제외 계측·출력 경로 격리, JP 재생성, 80자 잘림, TTS 실패, 앱 예외, Qwen 폴백, 입력 번역 실패, 키 메모리 전달·제거, 숨김 입력 불가 차단, 비TTY 입력 차단. 네트워크·재생은 준비 검증에서 mock했고 실환경 검수로 표시하지 않는다. 프로젝트 64개 테스트는 실행 코드 변경이 없어 이번에 반복하지 않았다.

현재 포트 확인: Ollama PID 1198/11434, Python PID 3352/5001 LISTEN. 이는 준비 상태의 증거이며 실제 새 음성 합성 성공을 대신하지 않는다. 커밋·푸시·패키지·모델 변경 없음.

다음 실행 명령:

```bash
/Users/eonpisa_1017/Desktop/AI_Waifu_System/.venv/bin/python -B /private/tmp/ai-waifu-gemini-guqz37yp/check_conversation.py
```

`너:`가 나온 후 첫 입력: `안녕. 오늘 학교에서 시험을 봤어. 한 문장으로 짧게 답해 줘.`

현재 성공 0/4턴. 같은 문장으로 수정 전 실패를 재검수하고, 첫 턴 결과를 확인한 뒤 같은 세션에서 다음 입력을 안내한다. 사용자가 명령과 출력 예시를 혼동하지 않도록 실행할 명령만 명령 코드 블록으로 안내한다. 연말 마감과 가용 시간 미정은 유지한다.


## 19. 연속 첫 턴의 Gemini 401과 동일 키 사전 확인 추가 (2026-09-08)

사용자 실행 세션: `/private/tmp/ai-waifu-conversation-4qix2lpq`. 첫 입력은 기존의 세 문장이다.

| 단계 | 관측 결과 | 시간 |
| --- | --- | --- |
| 입력 보정·KO→JP | 원문 유지·정상 통과. 이전 일본어 문장부호 오판 수정이 실대화에서도 확인됨. | 12.100초 |
| JP 생성·검증 | 재생성 1회 후 검증 통과 | 3.808초 |
| 자막 | Gemini `gemini_http_401` → Qwen `qwen3.5:9b` 채택. 형식 검증은 통과했지만 어깨를 주물러 주겠다는 표현의 한국어 활용이 어색함. | 2.412초 |
| SBV2 합성 | 성공·WAV 검사 통과, 80자 잘림 없음 | 6.864초 |
| 재생 | 재생 함수 성공, 사용자 청취 판정은 아직 전달받지 않음 | 4.866초 |
| 전체·다음 입력 | **30.057초**, 다음 입력 복귀 YES, 이전 AI 답변 0개 | 재생 포함 |

첫 턴 기계 검수 FAIL 및 번역 품질 미통과. Gemini 경로 성공이 필수이므로 4턴 성공 수는 여전히 0/4다. 자막 실패 시 TTS와 다음 입력까지 진행하는 이번 대체 경로는 확인했지만, 모든 오류 복구 검수 완료로 일반화하지 않는다.

### 확인한 내용과 미확정 원인

Google 공식 API 오류 문서에서 401은 인증 오류로 분류한다: https://ai.google.dev/gemini-api/docs/api-errors . 현재 코드와 임시 실행기 비교에서 두 실행기는 같은 Gemini 어댑터·환경변수·요청 헤더 방식을 사용한다. 조사한 앱 코드에서 Gemini 키를 덮어쓰는 경로는 발견하지 못했다. 두 실행기가 각각 키를 숨김 입력받았기 때문에, 이전 단건 PASS 때와 같은 값이 전달됐는지는 기록 없이 소급 확인할 수 없다. 잘못 붙여 넣었다고 단정하지 않는다. 키 상태나 인증 서비스의 상세 원인도 응답 본문·계정 상태를 확인하지 않아 미확정이다. 키나 응답 원문 전체를 추가로 수집하지 않았다.

### 임시 검수 흐름 보완

프로젝트 코드는 수정하지 않고 `/private/tmp/ai-waifu-gemini-guqz37yp/check_conversation.py`만 보완했다. 숨김 키 입력 1회 → 기존 `translator.japanese_to_korean()`으로 고정 문장 단건 사전 확인 → 지정 Gemini 모델 채택·폴백 없음일 때만 같은 프로세스의 4턴 시작 순서다. 사전 확인에서 Qwen 폴백이나 실패가 나면 대화·TTS를 시작하지 않고 종료한다. 사전 확인 시간은 턴 시간과 별도로 표시한다. 키는 프로세스 환경변수에만 두고 종료 시 제거하며 로그·파일에 저장하지 않는다.

모의 검증 **3건 통과**: 단 한 번 입력한 동일 키가 사전 확인과 네 턴의 모든 Gemini 요청에 전달됨, 사전 확인 HTTP 401이면 사용자 대화 입력·TTS 호출 없이 FAIL 종료, 사전 확인 이후 첫 대화에서 401이 나도 전체 성공으로 오판하지 않음. 키·헤더·응답 본문 비노출과 cwd·출력 경로 복원도 확인했다. 실제 API 키와 서비스를 사용한 검증은 아니며 사용자 재실행이 필요하다. 프로젝트 실행 코드는 변경하지 않아 기존 64개 테스트는 반복하지 않았다.

다음 조작은 기존 세션 `종료` → 갱신된 연속 검수기 실행이다. 별도 단건 실행기에 키를 다시 입력하고 또 연속 검수기에 입력하는 순환은 종료한다. 인증 통과를 먼저 확인한 후 연속 검수에 시간을 쓰도록 순서를 조정했으며, 연말 마감·사용자 가용 시간 미정은 유지한다. 커밋·푸시·패키지·모델 변경은 하지 않았다.

## 20. 동일 키 사전 확인과 첫 턴 기계 검수 PASS (2026-09-08)

사용자 실행 세션 `/private/tmp/ai-waifu-conversation-1dllpab8`. 단건 사전 확인에서 Gemini 지정 모델·fallback_reason=none·PASS, 1.382초 확인. 이는 네 턴 시간에 포함하지 않는다. 이전 401의 상세 원인은 여전히 미확정이다.

사용자는 이후 답변이 화면에 보이지 않았다고 알려왔다. 반복해서 사전 확인 출력만 전달된 것을 앱 입력 결함의 증거로 취급하지 않는다. 이번에는 실제 `[턴 1/4 검수 결과]`가 전달됐다.

| 단계 | 결과 | 시간 |
| --- | --- | --- |
| 입력 보정·번역 | 보정 없음, 번역 YES | 11.304초 |
| JP 생성·검증 | YES, 재생성 1회 | 3.606초 |
| 자막 검증 | YES | 1.027초 |
| SBV2 합성·WAV | 둘 다 YES | 6.239초 |
| 재생 | YES | 4.787초 |
| 전체 | **26.968초**, 다음 입력 복귀 YES, 80자 잘림 NO, 이전 AI 답변 0개 | 입력 대기 제외·재생 포함 |

기계 검수 PASS가 전달됐다. 사용한 임시 실행기의 PASS 조건은 Gemini 지정 모델 채택·폴백 없음까지 포함하지만, 이번 메시지에는 턴별 provider 로그 자체가 없어 직접 채택 로그 증거는 추가로 받는다. JP·KO 텍스트와 사용자 청취 판정도 없어 의미·말투·발음·음질 검수는 아직 대기다. 시간 합계의 작은 차이는 호출 사이 처리·표시 및 계측 구간 차이이며 성능 목표를 확정하지 않는다.

현재 **기계 검수 1/4, 전체 품질 검수 완료 0/4**. 같은 세션에서 두 번째 입력으로 시험 이야기의 문맥과 위로 반응을 확인한다. 문장: `시험이 어려워서 조금 힘들었어. 짧게 위로해 줘.` 다음 전달 자료는 JP·KO 표시, 번역기 로그, 턴 요약, 청취 판정이다. 첫 턴 누락 자료도 함께 받되 완료한 기계 검수를 다시 실행할 필요는 없다.

이번에는 계획 기록만 갱신했다. 앱 코드·임시 실행기는 수정하지 않았고 자동 테스트도 반복하지 않았다. 세션 재시작·키 재입력 없이 이어가며 연말 마감과 가용 시간 미정은 유지한다.

## 21. 두 번째 턴 기계 검수 PASS (2026-09-08)

사용자가 같은 세션의 `[턴 2/4 검수 결과]`를 전달했다. JP·KO 본문, 턴별 번역기 채택 로그, 청취 의견은 이번에도 포함되지 않았다.

| 단계 | 결과 | 시간 |
| --- | --- | --- |
| 입력 보정·번역 | 보정 없음, 번역 YES | 4.969초 |
| JP 생성·검증 | YES, 재생성 0회 | 2.227초 |
| 자막 검증 | YES | 1.131초 |
| SBV2 합성·WAV | 둘 다 YES | 6.035초 |
| 재생 | YES | 6.382초 |
| 전체 | **20.746초**, 다음 입력 복귀 YES, 80자 잘림 NO | 입력 대기 제외·재생 포함 |

이전 AI 답변 **1개가 요청에 포함**됐다. 이는 history 전달의 증거이며, 응답이 실제로 문맥을 이해했는지의 판정은 아니다. 기계 PASS는 실행기의 Gemini 지정 모델 채택 조건을 포함하지만 직접 채택 로그는 추가 확인이 필요하다. 현재 **기계 검수 2/4, 품질 검수 완료 0/4**다. 단일 턴 시간 차이로 성능 향상을 주장하지 않는다.

다음 문장: `고마워. 내일 은 친구와 산책하기로 했어. 짧게 답해 줘.` 의도적인 `내일 은` 띄어쓰기를 어떻게 처리하는지와 다음 날 계획 전달을 확인한다. 모델이 보정하지 않아도 결과와 의미를 먼저 확인하며, 보정 동작을 자동 완료로 표시하지 않는다. 이후 네 번째 턴에서 앞선 시험·산책 이야기를 함께 확인할 예정이다.

사용자에게 요약 부분만이 아니라 `입력값:`부터 해당 턴 끝까지의 화면 출력과 음성 이상 여부를 받도록 안내한다. 채팅에서 공유된 검수 내용을 추가 원문 로그 파일로 복제하지 않는다. 코드·모델·패키지 변경이나 테스트 반복 없이 계획만 갱신했다. 연말 마감·가용 시간 미정은 유지한다.

## 22. 세 번째 턴 기계 검수 PASS (2026-09-08)

사용자가 같은 세션의 `[턴 3/4 검수 결과]`를 전달했다. 입력 보정 적용이 보고됐으나 보정 전후 본문은 없어 `내일 은`이 정확히 어떻게 바뀌었는지는 확인하지 못했다.

| 단계 | 결과 | 시간 |
| --- | --- | --- |
| 입력 보정·번역 | 보정 적용, 번역 YES | 5.328초 |
| JP 생성·검증 | YES, 재생성 0회 | 1.991초 |
| 자막 검증 | YES | 1.149초 |
| SBV2 합성·WAV | 둘 다 YES | 6.219초 |
| 재생 | YES | 5.767초 |
| 전체 | **20.455초**, 다음 입력 복귀 YES, 80자 잘림 NO | 입력 대기 제외·재생 포함 |

이전 AI 답변 **2개가 요청에 포함**됐다. 기계 검수는 3/4턴 PASS이며, 실제 내용·말투·청취 품질은 아직 판정하지 않았다. 개별 provider 로그도 미수신이며 Gemini 채택은 실행기의 PASS 조건을 통한 간접 증거다. 요약 정보만으로 품질 검수를 완료 처리하지 않는다.

마지막 입력은 `내가 오늘 학교에서 뭘 했고, 내일은 누구와 뭘 하기로 했지? 한 문장으로 짧게 말해 줘.`다. 답변이 오늘 학교에서 시험을 봤다는 사실과 내일 친구와 산책한다는 계획을 보존하는지 확인한다. 이는 현재 세션의 문맥 검사이며 영구 기억 기능 검수가 아니다. 마지막 턴은 다음 입력 함수 도달을 확인한 뒤 실행기가 `종료`를 공급해 세션을 마친다.

이번에는 계획 기록만 갱신했으며 실행 코드는 변경하지 않았다. 남은 작업은 네 번째 턴과 누락된 품질 증거 확인이다. 전체 일정·가용 시간은 변경하지 않는다.

## 23. 4턴 연결 검수 완료·의미와 문맥 검수 결과 (2026-09-08)

사용자가 동일 세션 `/private/tmp/ai-waifu-conversation-1dllpab8`의 전체 화면 출력을 전달했다. 앞서 누락됐던 각 턴의 JP·KO 본문, 입력 보정 결과와 직접 provider 로그를 이번에 확인했다. 전달된 내용을 별도의 원문 로그 파일로 복제하지 않고 아래 판정과 수치만 기록한다. 사용자 청취 의견은 아직 없다.

### 연결·시간 결과

| 턴 | KO→JP | JP 생성·검증 | Gemini 자막 | SBV2 합성 | 재생 | 전체 | JP 재생성 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 11.304초 | 3.606초 | 1.027초 | 6.239초 | 4.787초 | 26.968초 | 1회 |
| 2 | 4.969초 | 2.227초 | 1.131초 | 6.035초 | 6.382초 | 20.746초 | 0회 |
| 3 | 5.328초 | 1.991초 | 1.149초 | 6.219초 | 5.767초 | 20.455초 | 0회 |
| 4 | 6.336초 | 1.343초 | 0.855초 | 6.091초 | 5.233초 | 19.859초 | 0회 |

4턴 모두 `provider=gemini model=gemini-3.5-flash-lite fallback_reason=none` 직접 로그 확인. 합성·WAV·재생·다음 입력 복귀 모두 YES, 80자 잘림 모두 NO. 이전 AI 답변 포함 개수는 0→1→2→3이다. 마지막 다음 입력 함수 도달 뒤 실행기가 `종료`를 전달했고 `4턴 기계 검수: PASS`로 정상 종료했다. 사전 확인 1.382초는 네 턴에 포함하지 않는다.

전체 평균 **22.007초**, 범위 **19.859–26.968초**, Gemini 자막 시간은 **0.855–1.149초**다. 서로 다른 문장 4건의 기술 통계일 뿐 cold/warm 통제나 p95 검수 결과가 아니며, 장비의 보장 지연 목표를 정하지 않는다.

### 품질 판정

| 검수 항목 | 판정·근거 | 담당 경로 |
| --- | --- | --- |
| 한국어 보정 | 세 번째 입력의 `내일 은`→`내일은` 확인. 다른 간격도 바뀌었으나 전달 의미는 유지. | 입력 보정 |
| KO→JP 의미·관점 | 핵심 시험·산책 사실은 전달됨. 두 번째 입력의 일본어 종결과 네 번째 질문의 주어 생략은 화자 관점을 흐릴 여지가 있어 후속 평가 대상으로 기록. 이것만으로 마지막 실패 원인을 확정하지 않음. | `translator.py` 입력 번역 |
| Gemini JA→KO | 네 건 모두 생성된 JP의 핵심 의미를 옮겼고 혼합 언어·설명문·심한 오역·문장 깨짐 없음. 세 번째 자막의 직역 표현과 의문형 어미는 자연스러움 개선 여지. | Gemini 자막 |
| 일본어 자연스러움 | 첫 턴의 수고 인사에 의문형 어미가 붙는 어색함. 문자·언어 검증 통과와 문법 품질은 다름. | 일본어 응답 생성 |
| 위로·캐릭터 말투 | 두 번째 턴은 사용자가 울었다는 근거 없이 울기만 하면 안 된다는 표현을 생성. 부드러운 위로 기준 미달. 존댓말/반말의 일관성도 후속 평가 필요. | 대화 모델·페르소나 |
| 대화 사실 회상 | **실패**. 기대는 오늘 학교에서 시험, 내일 친구와 산책인데 응답은 오늘 공부와 내일 캐릭터가 사용자와 이야기하고 싶은 희망으로 바뀜. | 일본어 응답 생성·문맥 처리 |
| 음성 | 기계 합성·재생 성공. 실제 발음·잡음·끊김 판정은 사용자 확인 대기. | SBV2·청취 |

마지막 턴은 일본어 응답부터 사실이 잘못됐고 Gemini 자막은 그 응답을 충실히 번역했다. 따라서 이를 Gemini의 심한 오역으로 분류하지 않는다. 현재 세션 문맥 실패이며 영구 기억 미구현만으로 설명하지 않는다.

코드를 재확인했다. `main.py`는 사용자 발화·검증된 일본어 답변을 history에 추가하고 `llm.py`는 전달받은 messages를 Ollama 요청에 넣는다. 네 턴은 MAX_HISTORY 범위 안이며 현재 코드에서 history를 버리는 경로는 발견되지 않았다. 다만 사용자 실행의 실제 HTTP 요청 본문을 수집한 것은 아니다. 검증기는 문자·언어·형식 위주이므로 문맥 사실과 위로의 적절성을 보장하지 않는다. 모델의 실패가 프롬프트만으로 해결될지는 아직 검증하지 않았다.

### 추가 확인과 상태

- 현재 코드의 `.venv/bin/python -B -m unittest discover -s tests -q`: **64개 / 0.032초 / OK**. 품질 실패를 자동 테스트가 탐지했다고 표현하지 않는다.
- 네 WAV를 read-only 파싱: 모두 44,100Hz·mono·16bit PCM. 음성 길이는 각각 3.042/4.807/4.609/3.762초. 재생 함수 시간에는 플레이어 실행 등의 시간이 더 포함되므로 파일 길이와 구분한다. 청취 판정을 대신하지 않는다.
- **기계 연결 4/4 완료, Gemini 자막 경로·의미 검수 완료, 전체 캐릭터 대화 품질 미통과, 청취 대기**. 사용자 원래 목표의 전체 완료로 처리하지 않는다.
- 앱 코드·패키지·모델 변경, 커밋·푸시 없음. 이번에는 계획 기록만 갱신했다.

### 우선순위와 남은 일

다음 개발은 Q05의 작은 선행 작업을 P0로 올려 이번 화자·사실 회상·위로 실패를 다룬다. 입력 번역의 화자·시제 유지 지시, 대화 생성의 사용자 사실 우선·근거 없는 감정 단정 방지 지시를 최소 수정 후보로 검토한다. 실제 코드 적용은 구체 수정안 설명·사용자 확인 후 진행한다. 사례에 맞춘 고정 답변이나 단어 존재 검사만으로 기억 기능이 완료됐다고 판단하지 않는다.

이 후속 작업은 초기 추정 집중시간 1–2시간, 모델 반응에 따른 불확실성 중간–높음이며 실제 재현 검수 후 재추정한다. 새로운 장비·패키지·API 도입은 현재 필요하다고 판단하지 않는다. 사용자 다음 행동은 네 턴의 음성 이상 여부 한 번 확인이다. 연말 마감과 가용 시간 미정은 유지하며, 전체 연결 검수와 SBV2 성공 시험을 반복하지 않는다. 향후 문맥·말투를 고친 부분만 우선 재검수한다.

## 24. 실행 중 문맥·화자 유지 최소 수정 (2026-09-08)

사용자 적용 승인 후 main.py와 translator.py를 수정했다. 영구 저장·불러오기 기능은 추가하지 않았다.

- 사용자 history에는 번역된 일본어 발화만 넣는다. 한국어 포장 문구와 매 턴 스타일 지시가 사용자 발화로 누적되던 구성을 해소했다. 현재 스타일은 요청 사본의 system에만 추가한다.
- system에 화자 구분, 사용자 사실의 출처, 최근 명시적 정정 우선, 질문에 직접 답하기, 근거 없는 경험·감정 단정 금지를 추가했다. 사례별 시험·산책 정답을 프롬프트에 넣지 않았다.
- KO→JA 프롬프트에서 사용자 입장 번역, 명시적 1인칭 유지, 시제·동행·계획 보존을 지시했다. 프롬프트 개선만으로 품질을 보장하지 않는다.
- llm.py, 일본어 검증·재생성 구현, Gemini 경로, SBV2 코드는 이번에 수정하지 않았다. tts.py·tts_config.json·SBV2 config.yml·bert_models.py의 SHA256은 직전 성공 상태와 일치했다.

검증 결과:

- 실제 main.py 루프의 메시지 역할·스타일 격리·재생성 전달과, 검증 실패 턴이 다음 이력·자막·TTS에 섞이지 않는 회귀 테스트 2개 추가. 기존 64개 포함 **66개 / 0.041초 / OK**. 테스트의 콘솔 로깅 설정은 격리했다.
- 실제 Ollama KO→JA와 일본어 생성으로 기존 네 입력을 실행했다. Gemini·합성·재생은 이 텍스트 시험에서 대역 처리했으므로 성공 증거로 계산하지 않는다. 일본어 응답 4건 유효, 이전 assistant 수 0/1/2/3, 최종 history 역할 4쌍 유지. 마지막 응답의 시험·내일·친구·산책 단어 확인 모두 참. 원문 출력·저장 없이 단어 검사만 했으므로 화자와 전체 의미 검수는 미완료다.
- 일본어 생성·검증 구간만 2.888/3.175/3.215/1.391초. 전체 응답 시간과 비교하지 않는다. 최초 sandbox 연결 실패 후 권한을 받아 localhost 재검수했다.
- 수정 대상 diff의 CRLF를 허용한 whitespace 검사 통과. 저장소 전체 기본 diff 검사는 기존 CRLF 파일들에 경고가 있어 전체 통과로 기록하지 않는다.
- 사라진 임시 실행기를 대신해 `/private/tmp/ai-waifu-context-VuUhYG/check_conversation.py` 준비. getpass → 프로세스 환경변수 → 같은 키 Gemini 사전 확인 → 실제 main.py 4턴. WAV는 별도 임시 폴더, 로그는 provider/model/fallback 사유만 출력, 본문은 앱 화면 표시만 하며 파일로 기록하지 않는다. 원래 output.wav를 덮어쓰지 않는다. 구문 검사 완료, 실제 Gemini·음성은 사용자 실행 대기.

| ID | 작업·결과물 | 우선순위·선행 | 상태·담당 | 남은 시간·불확실성 | 완료·검수 기준 |
| --- | --- | --- | --- | --- | --- |
| Q05-A | 메시지/화자 최소 수정과 회귀 테스트 | P0·승인 | Codex 구현·자동 검증 완료 | 구현 잔여 없음 | 66개 통과, 기존 음성 경로 보존 |
| Q05-B | 수정 후 4턴 의미·자막·음성 검수 | P0·Q05-A | Codex 텍스트 예비 검사 완료, 사용자 입력·청취 대기 | 15–30분, 중간 | 마지막에 사용자 시험·친구와 산책 정확히 회상, 매 턴 Gemini none, SBV2 재생·입력 복귀 |

우선순위는 Q05-B 검수이며 연말 마감·가용 시간 미정은 유지한다. 추가 수정 필요 여부는 의미 검수 결과로 결정한다. 전체 페르소나 품질이나 영구 기억이 완료됐다고 판단하지 않는다. 커밋·푸시·패키지·모델 변경 없음.

## 25. 수정 후 사용자 4턴 검수 결과

사용자가 local_checks/check_conversation.py 실행 결과를 네 턴 모두 전달했다. 사전 확인 1.090초, Gemini 지정 모델 채택 후 동일 프로세스에서 대화했다. 과거 절의 사용자 실행 대기 상태는 아래 결과로 갱신한다. 원문 대화·키·요청 헤더는 문서에 복제하지 않는다.

| 턴 | 문맥·의미 | Gemini | 재생 함수·입력 복귀 | 전체 시간(재생 포함) |
| --- | --- | --- | --- | --- |
| 1 | 시험 사실 반영. 안도 감정은 사용자 진술 없이 추정한 표현으로 개선 여지 | 지정 모델·폴백 없음 | PASS·YES | 20.072초 |
| 2 | 힘들었다는 말에 부드러운 위로. 과거의 울었다는 단정·꾸짖음 없음 | 지정 모델·폴백 없음 | PASS·YES | 19.087초 |
| 3 | 내일 은→내일은 보정, 친구와 산책 계획의 일본어 전달 정상. 자막의 다녀오렴은 말투 차이 | 지정 모델·폴백 없음 | PASS·YES | 16.761초 |
| 4 | 오늘 시험·내일 친구와 산책 정확히 회상. 캐릭터 자신의 희망으로 바뀌던 기존 실패는 이번 사례에서 재현되지 않음 | 지정 모델·폴백 없음 | PASS·YES | 15.031초 |

모든 턴 직접 로그에 provider=gemini, model=gemini-3.5-flash-lite, fallback_reason=none 확인. 자막에 혼합 언어·설명문·심한 오역·문장 깨짐은 관찰되지 않았다. 일본어 답변이 자막·TTS 단계에 도달했고 음성 재생 함수가 성공했다. 실행기가 종료 입력을 전달한 뒤 셸로 정상 복귀했다. 일본어 재생성 횟수는 이 실행기 출력에 없으므로 0회로 단정하지 않는다.

전체 평균 17.738초, 범위 15.031–20.072초(n=4). 이전 4턴 평균보다 짧지만 입력별 응답 길이와 워밍업 등이 통제되지 않아 수정으로 속도가 개선됐다고 결론 내리지 않는다.

마지막 KO→JP 질문은 명시적 1인칭을 여전히 생략했다. 따라서 프롬프트가 항상 주어를 보존한다고 판단하지 않는다. 이번 답변은 문맥에서 사용자 사실을 올바르게 회상했으며, 영구 기억 구현 또는 광범위한 문맥 정확도 보장의 증거는 아니다.

Q05-B 상태: **완료**. 문맥 사례·자막 의미·재생 함수·정상 종료 검수 후 사용자가 네 턴 음성 모두 이상 없음을 확인했다. 해당 검수의 남은 작업은 없으며 같은 연결·청취 시험을 반복하지 않는다. 연말 마감 유지. 이번에는 기록만 갱신했으며 코드 변경·테스트 재실행·커밋·푸시 없음. 이 완료 판정은 이번 4턴 사례에 한정하며 영구 기억이나 전체 페르소나 품질 완료를 뜻하지 않는다.

## 26. 정상 실행 기준 고정

README에 SBV2와 앱의 작업 폴더·Python·설정·안전한 키 입력·종료·테스트 명령을 정리했다. 키를 명령 문자열에 직접 적던 예시는 제거했다. 구현 코드는 변경하지 않았고 설치·모델 교체·STT·영구 기억·커밋·푸시도 하지 않았다.

확인한 기준:

- Python: 프로젝트 `.venv/bin/python`, 3.11.0. 설치 버전 transformers 4.51.3, torch 2.5.1, requests 2.34.2, uvicorn 0.30.0. 버전 고정 파일이나 재현 가능한 전체 lock을 새로 만든 것은 아니다.
- SBV2: SBV2-KR-master 폴더에서 `../.venv/bin/python -B server_fastapi.py`. server.port=5001, --port 미지원. paths.yml의 assets_root=model_assets. 현재 서버는 CUDA가 없으면 CPU를 선택한다.
- 앱: 프로젝트 루트에서 가상환경을 활성화하고 `python -B main.py`. tts_config.json의 SBV2 주소 5001·backend sbv2·CosyVoice fallback false 확인. 환경변수 적용 후 유효 값도 같은 기준임을 bool 검사했다. local override 파일은 없고 자동 탐색하지도 않는다.
- Ollama: API 11434/api/chat, 대화·번역 qwen3.5:9b. Gemini: gemini-3.5-flash-lite, v1beta REST generateContent. 현재 Codex 프로세스에 키 없음(값 미출력); 이전 사용자 검사 프로세스의 키를 재사용할 수 있다고 간주하지 않는다.
- 설정 우선순위 주의: 환경변수가 JSON보다 우선. 설정이 누락되면 tts.py에 5000·CosyVoice 기본값이 남아 있으나 현재 정상 설정과 실행 명령에서는 선택되지 않는다. 앱 코드를 고칠 필요 없이 실행 명령에서 config 절대 경로·SBV2 backend/URL·fallback false를 명시했다. 방어적 코드 변경은 이번에 적용하지 않았다.
- main.py 재생은 상대 output.wav, tts.py 저장은 프로젝트 절대 경로이므로 앱 작업 폴더를 프로젝트 루트로 고정한다. 일반 실행은 output.wav를 교체한다. 임시 실행기의 WAV 경로 변경·로깅 래퍼는 일반 실행에 적용하지 않는다.

이번 확인 결과:

- 자동 테스트 **66개 / 0.020초 / OK**. 실패 모의 테스트의 warning은 실제 서버 장애 증거가 아니다.
- 5001 Python 리스너와 11434 Ollama 리스너 확인. sandbox 통신 제한 후 허용받은 읽기 요청으로 SBV2 /docs HTTP 200, Ollama /api/tags HTTP 200 및 qwen3.5:9b 존재 확인. 서버를 재시작하거나 중복 기동하지 않았다.
- README zsh 실행 블록 구문 검사 PASS. 키는 숨김 입력 후 서브셸 환경에서만 사용, 앱 종료 후 제거. 키·요청 헤더·대화 원문 로그 파일은 만들지 않는다.
- 사용자 검수 래퍼를 통한 4턴 문맥·Gemini·SBV2·청취 통과는 25절 결과로 유지한다. **이번 일반 python main.py 경로의 실사용 한 턴은 사용자 확인 대기**이며 기존 검수 결과로 대체하지 않는다.

남은 작업은 README의 앱 명령으로 한 턴을 실행하여 일본어 응답, Gemini 지정 모델·fallback_reason=none, 한국어 자막, 음성 정상 및 다음 입력 복귀를 확인하는 것(사용자 입력·청취 약 5–10분, 서버 상태에 따라 변동). 결과 수신 후 실행 기준 검수 완료로 갱신한다. 마감 2026-12-31 및 가용 시간 미정 유지. 새로운 기능 개발보다 이 확인을 우선한다.

일반 실행 첫 결과: 사용자가 `python main.py` 실행 후 한 턴을 전달했다. KO→JP·일본어 응답·음성 재생·다음 입력 복귀 확인, 사용자 청취 이상 없음. 자막 로그는 provider=qwen, model=qwen3.5:9b, fallback_reason=gemini_api_key_missing으로 Gemini 기준 미통과. 키 값이나 원문은 기록하지 않는다. 이전 검수 실행기의 환경변수는 해당 자식 프로세스 종료 후 새 앱에 전달되지 않는다. 이번 앱 프로세스에는 사용할 키가 없었다는 로그 근거만 확인하며, 사용자가 어떤 셸 설정 단계를 수행했는지는 단정하지 않는다. 코드 수정 없이 키 숨김 입력을 포함한 실행 블록으로 다시 시작해 확인한다. 이번 문서 갱신에서는 테스트 재실행 없음.

일반 실행 재검수: 사용자가 안내된 키 숨김 입력 실행 블록으로 앱을 시작하고 한 턴 결과를 전달했다. KO→JP·일본어 응답 정상, 직접 로그에서 provider=gemini, model=gemini-3.5-flash-lite, fallback_reason=none 확인. 한국어 자막은 일본어 응답 의미를 전달하며 혼합 언어·설명문·심한 오역 없음. 사용자 음성 정상 및 이후 다음 입력 복귀를 모두 확인했다. 키 누락 장애는 이 실행에서 해소됐다. 시간 측정값은 없다. **실행 기준 검수 완료**: SBV2와 앱 5001 일치, CosyVoice fallback 비활성화, 키를 파일에 저장하지 않는 실행 절차, 일반 main.py 한 턴 전 경로와 입력 복귀, 이번 작업의 자동 테스트 66개 통과. 앱 코드·서버 설정 변경 없이 문서만 갱신했으며 이번 마지막 기록 단계에서는 테스트 재실행·커밋·푸시 없음. 남은 검수 작업 없음. 마감·가용 시간 조건은 유지한다.
